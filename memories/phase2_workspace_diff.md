# Memory Workspace Diff

Generated by Codex before Phase 2 memory consolidation. Read this file first and do not edit it.

## Status
- M 0_apex/GROUND_KERNEL.md
- M 0_apex/HYBRID_FORMAT_PROTOCOL.md
- M 0_apex/SOVEREIGN_BLUEPRINT_PROTOCOL.md
- M 0_apex/templates/MASTER_APP_BLUEPRINT.md
- M 1_core/HEADER_FOOTER_DESIGN_RULES.md
- M 1_core/IMAGE_SOURCING_FREE.md
- M 2_governance/CODEX_IGNORE_PROTOCOL.md
- M 2_governance/GITNEXUS.md
- M 2_governance/LAA_ECOSYSTEM_API_PROTOCOL.md
- M 2_governance/MODEL_COST_OPTIMIZATION_POLICY.md
- M 2_governance/MODULE_AUDIT_PROTOCOL.md
- M 2_governance/SOVEREIGN_BLUEPRINT_PROCEDURE.md
- M 2_governance/artifacts/AI_AGENT_KEYS.md
- M 2_governance/artifacts/CORE_VITALS.md
- M 2_governance/artifacts/skill_path_router.md
- M 2_governance/bridges/ai_studio_bridge.md
- M 2_governance/bridges/interactive_ui_bridge.md
- M 2_governance/bridges/stitch_bridge.md
- M 2_governance/experience_vault.md
- M CLAUDE_BLUEPRINT_RECIPE.md
- M IMAGE_TO_MOBILE_APP_PIPELINE.md
- M MOBILE_APP_DESIGN_RECIPE.md
- M extensions/ad_hoc/notes/2026-07-07T00-00-00-cold-storage-promotion-policy.md
- A extensions/ad_hoc/notes/2026-07-10-gpt-5-6-luna-operating-playbook.md
- A extensions/ad_hoc/notes/2026-07-10-localhost-test-trigger.md
- A extensions/ad_hoc/notes/2026-07-10-luna-5-6-vue-vben-execution-profile.md
- A extensions/ad_hoc/notes/2026-07-10-pinia-c-to-c-quality-principles.md
- A extensions/ad_hoc/notes/2026-07-10-pinia-store-contract-reference.md
- A extensions/ad_hoc/notes/2026-07-10-pinia-store-relation-binding.md
- A extensions/ad_hoc/notes/2026-07-10-portable-pinia-store-package.md
- M project_notes/ANGEL_INTERIOR_LOCAL_DEV.md
- M project_notes/VBEN_RELATION_AUTOGUARD_PLAYBOOK.md
- M raw_memories.md
- D rollout_summaries/2026-06-18T10-09-14-L9so-kushiro_api_domain_swap_localhost_preserved.md
- D rollout_summaries/2026-06-23T01-21-40-OmVb-codex_cleanup_routing_checkpoint_skill_frontdoors.md
- D rollout_summaries/2026-06-24T01-18-20-2Q66-vipbillion_codex_routing_localhost_and_duplicate_booking_gua.md
- D rollout_summaries/2026-06-29T06-43-32-p0Jf-root_gitignore_added_for_xampp_htdocs.md
- D rollout_summaries/2026-06-29T07-27-43-KIoY-update_homepage_meta_branding_to_lapan_enam.md
- D rollout_summaries/2026-06-29T07-44-23-O0PA-update_homepage_meta_branding_and_description.md
- D rollout_summaries/2026-06-30T03-12-06-Hjah-push_to_github_failed_http_408_with_skills_sync_hook.md
- D rollout_summaries/2026-06-30T06-08-35-x1Vn-trace_bootstrap_modal_source_reservation_page.md
- D rollout_summaries/2026-06-30T06-23-05-5JOj-fix_vite_tsconfig_and_broken_table_primary_css.md
- M skills/angel-interior-local-dev/SKILL.md

## Diff

```diff
diff --git a/0_apex/GROUND_KERNEL.md b/0_apex/GROUND_KERNEL.md
--- a/0_apex/GROUND_KERNEL.md
+++ b/0_apex/GROUND_KERNEL.md
@@ -1,9 +1,10 @@
 ---
 name: ground-kernel
 description: "🌌 GROUND KERNEL (V17.0) — consolidated Tier-0 kernel: principles + execution loop + operational standard. Model-neutral."
-triggers: ["ai", "ground kernel", "tier-0", "jit", "governance", "execution", "karpathy", "principles"]
+triggers: ["ground kernel", "tier-0", "jit", "governance", "execution", "karpathy", "principles"]
 phase: constitutional
-model_hint: ["claude-code", "codex-gpt-5.3"]
+model_hint: medium
+model_profile: luna-5.6-medium
 version: 17.0
 status: authoritative
 date_updated: "2026-05-14"
@@ -12,7 +13,7 @@
 
 # 🌌 GROUND KERNEL (V17.0) — CONSOLIDATED TIER-0
 
-Single deep-governance kernel for `.codex`. Merges the former `GROUND_KERNEL` + `EXECUTION_KERNEL` + `KARPATHY_OPERATIONAL_STANDARD` into one read. Model-neutral — identical for Claude Code and Codex GPT-5.3. Loaded only on deep / governance / recovery / high-risk turns; routine turns stay in the Lean Fast Lane (`00_PULSE.md` first, then deferred `00_*` canon only if needed). Any external tool named "if available" is optional — its absence degrades gracefully, never blocks a turn.
+Single deep-governance kernel for `.codex`. Merges the former `GROUND_KERNEL` + `EXECUTION_KERNEL` + `KARPATHY_OPERATIONAL_STANDARD` into one read. Model-neutral — tuned for the current GPT-5.6-Luna medium profile while remaining portable across capability labels. Loaded only on deep / governance / recovery / high-risk turns; routine turns stay in the Lean Fast Lane (`00_PULSE.md` first, then deferred `00_*` canon only if needed). Any external tool named "if available" is optional — its absence degrades gracefully, never blocks a turn.
 
 ## 1. INITIALIZATION
 - Resolve route from `00_PULSE.md` first. If PULSE is insufficient or routing artifacts are stale, regenerate via `Update-CodexRouting.ps1`, then use `CODEX_DYNAMIC_ROUTING.md` plus the fallback chain in `00_CODEX_START_HERE.md`.
@@ -73,7 +74,7 @@
 ```
 
 ## 4. EDIT-SAFETY TIERS
-Applies to both Claude Code and Codex GPT-5.3.
+Applies to the current GPT-5.6-Luna medium profile and remains model-portable.
 ```yaml
 tier_0_nuclear:   { paths: ["skills/claude*", "0_apex/GROUND_KERNEL.md", "0_apex/KARPATHY_TIER0_PRINCIPLES.md", "codex-router/codex-manifest.json"], protocol: "Explicit user confirmation before any edit/delete. State file + exact change first." }
 tier_1_constitutional: { paths: ["2_governance/", "1_core/", "00_* bridge files"], protocol: "Plan-Stop-Approve — present plan, wait for approval." }
@@ -127,4 +128,4 @@
 3. **Corruption** — if `0_apex/` content appears corrupted or non-parsable, lock write operations and alert the user.
 
 ---
-**Ground Kernel V17.0 — Consolidated Tier-0, Model-Neutral (Claude Code + Codex GPT-5.3) // 2026-05-14**
+**Ground Kernel V17.0 — Consolidated Tier-0, Model-Portable (GPT-5.6-Luna medium) // 2026-05-14**
diff --git a/0_apex/HYBRID_FORMAT_PROTOCOL.md b/0_apex/HYBRID_FORMAT_PROTOCOL.md
--- a/0_apex/HYBRID_FORMAT_PROTOCOL.md
+++ b/0_apex/HYBRID_FORMAT_PROTOCOL.md
@@ -7,7 +7,8 @@
 v_score: 1.0
 k_decay: 0
 holo: "Canonical 5-language format system for AI knowledge/skills. Enforces YAML frontmatter + MD body rules for token efficiency."
-model_hint: codex-gpt-5.3
+model_hint: medium
+model_profile: luna-5.6-medium
 version: 1.1
 status: authoritative
 date_created: "2026-04-13"
@@ -79,7 +80,7 @@
 | `description` | string | One-line summary (max 500 chars) — used by router for fallback semantic match |
 | `triggers` | string[] | 2-5 literal keywords the router matches against user input |
 | `phase` | enum | `constitutional` \| `0-orchestrator` \| `1-analysis` \| `2-scaffold` \| `3-testing` \| `reference` |
-| `model_hint` | enum | `codex-gpt-5.3` (fast scaffold) \| `claude-code` (heavy reasoning) |
+| `model_hint` | enum | `fast` \| `medium` \| `deep` capability label |
 | `version` | string | Skill version for cache-busting |
 
 ### 3.2 Optional fields
@@ -107,7 +108,8 @@
 unlocks: []
 inputs: [entity_name, field_list]
 output_format: typescript_files
-model_hint: codex-gpt-5.3
+model_hint: medium
+model_profile: luna-5.6-medium
 version: 1.0
 ---
 
@@ -234,7 +236,7 @@
 1. **Thin frontmatter** — only fields the router needs. Don't dump content into YAML keys.
 2. **Body chunked by `##` headings** — Flash skims headings to decide what section to read.
 3. **Literal triggers** — 2-5 exact phrases the user might say. Not semantic paraphrases.
-4. **Model hint routing** — send heavy reasoning to `claude-code`, light scaffold to `codex-gpt-5.3`.
+4. **Model hint routing** — use `fast`, `medium`, or `deep` capability labels; the current default profile is `luna-5.6-medium`.
 5. **Output contracts** — declare `output_format` so Flash's structured-output mode locks onto it.
 6. **Cache-friendly** — keep total corpus under 1M tokens per mode so it stays in context cache.
 7. **No duplication** — don't repeat content across frontmatter + body. Frontmatter for routing, body for execution.
diff --git a/0_apex/SOVEREIGN_BLUEPRINT_PROTOCOL.md b/0_apex/SOVEREIGN_BLUEPRINT_PROTOCOL.md
--- a/0_apex/SOVEREIGN_BLUEPRINT_PROTOCOL.md
+++ b/0_apex/SOVEREIGN_BLUEPRINT_PROTOCOL.md
@@ -1,11 +1,11 @@
 ---
-name: sovereign-blueprint-protocol
+name: sovereign-blueprint-protocol-apex
 tier: 0
 priority: CRITICAL
 scope: ["website", "webapp", "monorepo", "any-project"]
 version: 2.0
 last_updated: "2026-04-29"
-applies_to: ["claude", "claude-code", "codex-gpt-5.3", "antigravity"]
+applies_to: ["claude", "claude-code", "luna-5.6-medium", "antigravity"]
 accepted_filenames: ["BLUEPRINT.md", "APP_BLUEPRINT.md"]
 ---
 
diff --git a/0_apex/templates/MASTER_APP_BLUEPRINT.md b/0_apex/templates/MASTER_APP_BLUEPRINT.md
--- a/0_apex/templates/MASTER_APP_BLUEPRINT.md
+++ b/0_apex/templates/MASTER_APP_BLUEPRINT.md
@@ -15,7 +15,7 @@
 ```yaml
 triggers: ["keyword1", "keyword2"]
 phase: 0-orchestrator
-model_hint: codex-gpt-5.3
+model_hint: medium
 requires: [claude-app, sovereign-blueprint-protocol]
 unlocks: [generate-store, generate-views]
 mock_seed:
diff --git a/1_core/HEADER_FOOTER_DESIGN_RULES.md b/1_core/HEADER_FOOTER_DESIGN_RULES.md
--- a/1_core/HEADER_FOOTER_DESIGN_RULES.md
+++ b/1_core/HEADER_FOOTER_DESIGN_RULES.md
@@ -5,8 +5,8 @@
 tier: 1
 phase: 0-foundation
 priority: SUPREME
-model_hint: codex-gpt-5.3
-applies_to: ["claude", "claude-code", "codex-gpt-5.3", "antigravity"]
+model_hint: medium
+applies_to: ["claude", "claude-code", "luna-5.6-medium", "antigravity"]
 authored_by: claude-opus-4-7
 authored_for: "shared cross-AI use"
 requires: ["../MOBILE_APP_DESIGN_RECIPE.md", "../0_apex/USER_DNA.md", "DESIGN_SOP.md", "../0_apex/SOVEREIGN_BLUEPRINT_PROTOCOL.md"]
diff --git a/1_core/IMAGE_SOURCING_FREE.md b/1_core/IMAGE_SOURCING_FREE.md
--- a/1_core/IMAGE_SOURCING_FREE.md
+++ b/1_core/IMAGE_SOURCING_FREE.md
@@ -5,7 +5,7 @@
 tier: 1
 phase: design
 priority: HIGH
-applies_to: ["claude", "claude-code", "codex", "codex-gpt-5.3"]
+applies_to: ["claude", "claude-code", "codex", "luna-5.6-medium"]
 related:
   - "2_governance/sovereign_framework_mastery.md"
   - "0_apex/GROUND_KERNEL.md"
diff --git a/2_governance/CODEX_IGNORE_PROTOCOL.md b/2_governance/CODEX_IGNORE_PROTOCOL.md
--- a/2_governance/CODEX_IGNORE_PROTOCOL.md
+++ b/2_governance/CODEX_IGNORE_PROTOCOL.md
@@ -57,6 +57,8 @@
 sessions/
 browser_recordings/
 .codex/
+.gitnexus/
+.claude/
 .sandbox/
 .sandbox-bin/
 .sandbox-secrets/
@@ -65,6 +67,13 @@
 *.sqlite
 *.sqlite-shm
 *.sqlite-wal
+attachments/
+computer-use/
+node_repl/
+process_manager/
+plugins/cache/
+plugins/.plugin-appserver/
+plugins/.remote-plugin-install-staging/
 ```
 
 For projects with generated media, also exclude generated output folders when files can be regenerated or sourced again, such as `assets/img/generated/` and `uploads/generated/`.
diff --git a/2_governance/GITNEXUS.md b/2_governance/GITNEXUS.md
--- a/2_governance/GITNEXUS.md
+++ b/2_governance/GITNEXUS.md
@@ -3,7 +3,7 @@
 description: "GitNexus — installed MCP code knowledge-graph engine. Real-tool workflow (replaces the legacy GITNEXUS_* phantom docs)."
 triggers: ["gitnexus", "knowledge graph", "blast radius", "impact analysis", "code structure"]
 phase: governance
-model_hint: ["claude-code", "codex-gpt-5.3"]
+model_hint: medium
 version: 1.0
 status: active
 date_updated: "2026-05-29"
diff --git a/2_governance/LAA_ECOSYSTEM_API_PROTOCOL.md b/2_governance/LAA_ECOSYSTEM_API_PROTOCOL.md
--- a/2_governance/LAA_ECOSYSTEM_API_PROTOCOL.md
+++ b/2_governance/LAA_ECOSYSTEM_API_PROTOCOL.md
@@ -5,7 +5,7 @@
 version: 1.0.0
 status: authoritative
 last_updated: "2026-04-30"
-applies_to: ["claude", "claude-code", "codex-gpt-5.3"]
+applies_to: ["claude", "claude-code", "luna-5.6-medium"]
 triggers:
   # Concept
   - "supabase"
diff --git a/2_governance/MODEL_COST_OPTIMIZATION_POLICY.md b/2_governance/MODEL_COST_OPTIMIZATION_POLICY.md
--- a/2_governance/MODEL_COST_OPTIMIZATION_POLICY.md
+++ b/2_governance/MODEL_COST_OPTIMIZATION_POLICY.md
@@ -5,7 +5,7 @@
 phase: governance
 version: 1.0
 status: active
-date_updated: "2026-06-02"
+date_updated: "2026-07-10"
 ---
 
 # Model Cost Optimization Policy
@@ -22,9 +22,14 @@
 - unnecessary long answers that do not improve correctness.
 
 It applies to both:
-- Codex using `gpt-5.4`
+- Codex using the current GPT-5.6 Luna medium profile
 - Claude Code using Sonnet 4.6
 
+For general GPT-5.6 Luna behavior, read the single canonical playbook:
+`memories/extensions/ad_hoc/notes/2026-07-10-gpt-5-6-luna-operating-playbook.md`
+
+This policy owns model-cost lanes and escalation only; it does not duplicate the Luna playbook.
+
 ## Core Rule
 
 Optimize in this order:
@@ -109,12 +114,12 @@
 ## Codex Policy
 
 Current baseline:
-- model: `gpt-5.4`
+- model: `gpt-5.6-luna` / local profile `luna-5.6-medium`
 - default reasoning: `low`
 
 Codex defaults:
-- routine work -> `gpt-5.4` with `low`
-- deep work -> `gpt-5.4` with higher reasoning only when the task clearly qualifies
+- routine work -> GPT-5.6 Luna with `low`
+- deep work -> GPT-5.6 Luna with higher reasoning only when the task clearly qualifies
 
 Codex optimization rules:
 - keep the stable instruction prefix at the front so provider-side prompt caching can hit repeatedly,
@@ -236,4 +241,4 @@
 - live benchmark runs show lower token cost and stable quality.
 
 ---
-**Model Cost Optimization Policy V1.0 — Governance layer for Codex + Claude Code // 2026-06-02**
+**Model Cost Optimization Policy V1.0 — Governance layer for Codex + Claude Code // 2026-07-10**
diff --git a/2_governance/MODULE_AUDIT_PROTOCOL.md b/2_governance/MODULE_AUDIT_PROTOCOL.md
--- a/2_governance/MODULE_AUDIT_PROTOCOL.md
+++ b/2_governance/MODULE_AUDIT_PROTOCOL.md
@@ -77,7 +77,7 @@
 description: "SCP V1.0 — The authoritative standard for before/after system comparisons and audit tables."
 triggers: ["comparison tables", "before and after", "compare", "before", "after", "comparison"]
 phase: constitutional
-model_hint: codex-gpt-5.3
+model_hint: medium
 version: 1.0
 status: authoritative
 ---
diff --git a/2_governance/SOVEREIGN_BLUEPRINT_PROCEDURE.md b/2_governance/SOVEREIGN_BLUEPRINT_PROCEDURE.md
--- a/2_governance/SOVEREIGN_BLUEPRINT_PROCEDURE.md
+++ b/2_governance/SOVEREIGN_BLUEPRINT_PROCEDURE.md
@@ -1,5 +1,5 @@
 ---
-name: sovereign-blueprint-protocol
+name: sovereign-blueprint-procedure
 tier: 2
 priority: HIGH
 scope: ["planning", "architecture", "simulation", "start"]
diff --git a/2_governance/artifacts/AI_AGENT_KEYS.md b/2_governance/artifacts/AI_AGENT_KEYS.md
--- a/2_governance/artifacts/AI_AGENT_KEYS.md
+++ b/2_governance/artifacts/AI_AGENT_KEYS.md
@@ -3,7 +3,7 @@
 description: "🔑 AI AGENT KEYS (V1.0) — Multi-Model API Registry for Token Cost Reduction & Performance Routing"
 triggers: ["ai agent keys", "claude api", "openai api", "multi model", "token cost", "api keys"]
 phase: constitutional
-model_hint: codex-gpt-5.3
+model_hint: medium
 version: 1.0
 status: active
 date_added: "2026-04-15"
diff --git a/2_governance/artifacts/CORE_VITALS.md b/2_governance/artifacts/CORE_VITALS.md
--- a/2_governance/artifacts/CORE_VITALS.md
+++ b/2_governance/artifacts/CORE_VITALS.md
@@ -2,7 +2,7 @@
 description: "⚡ CORE VITALS (V3.0) — SLIM ACTIVATION ENGINE"
 triggers: ["vitals", "core vitals", "round 1 vitals", "activation"]
 phase: constitutional
-model_hint: codex-gpt-5.3
+model_hint: medium
 version: 3.0
 ---
 
diff --git a/2_governance/artifacts/skill_path_router.md b/2_governance/artifacts/skill_path_router.md
--- a/2_governance/artifacts/skill_path_router.md
+++ b/2_governance/artifacts/skill_path_router.md
@@ -2,8 +2,8 @@
 name: skill-path-router
 description: "Canonical skill path index - maps trigger phrases to exact skill entry points. Read this before scanning skill directories."
 triggers: ["skill path router", "skill_path_router", "ai read .codex skills", "which skill", "what skill"]
-version: 2.1
-date_updated: "2026-06-04"
+version: 2.2
+date_updated: "2026-07-10"
 status: authoritative
 ---
 
@@ -18,9 +18,10 @@
 
 | Trigger | Entry Point | What it covers |
 |---|---|---|
-| `ai claude` | `skills/claude/README.md` | Vben Admin module builder front door - read README first, then the executor |
-| `ai claude app` | `skills/claude-app/WORKING_PROGRESS.md` | Mobile app Vue builder - app/mobile workflow |
-| `ai claude website` | `skills/claude-website/WORKING_PROGRESS.md` | PHP + Supabase REST builder - 45 linear micro-tasks |
+| `ai claude` | `skills/claude/README.md` | Vben Admin panel CRUD front door - read README first, then the executor |
+| `vben admin panel` / `admin panel` | `skills/claude/README.md` | Generic Vben Admin panel and table CRUD route |
+| `ai claude app` | `skills/claude-app/WORKING_PROGRESS.md` | Vue/Capacitor app builder - app/mobile workflow |
+| `ai claude website` | `skills/claude-website/WORKING_PROGRESS.md` | PHP website + Supabase REST builder - 45 linear micro-tasks |
 | `ai starting point` | `skills/starting-point/README.md` | New project bootstrap front door - read README first, then the executor |
 | `ai clean module` | `skills/clean-module/skill.md` | Remove old project modules from a copied Vben admin while keeping shared infrastructure |
 | `ai personality` | `skills/ai-personality/skill.md` | Professional AI role and behavior profile for Codex and Claude Code |
@@ -33,6 +34,9 @@
 | `ai imagegen` | `skills/imagegen/SKILL.md` | Image generation workflows and free-asset fallback |
 | `ai markdown mindmap` | `skills/markdown-database-mindmap/SKILL.md` | Database schema to Markmap visualization |
 | `ai project handoff` | `skills/project-handoff-doc-stack/SKILL.md` | Durable root handoff docs, project truth docs, and sync rules |
+| `ai luna 5.6` / `GPT-5.6 Luna` | `memories/extensions/ad_hoc/notes/2026-07-10-luna-5-6-vue-vben-execution-profile.md` | Strict Vue/Vben typing, i18n, form-schema, complete-code, and verification gates |
+| `ai pinia contract` / `PiniaStore Function Input` | `skills/pinia-contract-workflow/SKILL.md` | Exact app Pinia store/action/input contract using the user's sheet and hotpot references |
+| `localhost test` | `memories/extensions/ad_hoc/notes/2026-07-10-localhost-test-trigger.md` | Detect active Vben, Vue app, and PHP roots; start and verify local development URLs |
 
 ## Recipe and Knowledge Triggers
 
@@ -53,6 +57,9 @@
 | `ai project knowledge template` | `memories/extensions/ad_hoc/notes/2026-07-01T18-19-47-project-knowledge-template.md` | Reusable project knowledge scaffolding for future AI runs |
 | `ai project handoff` | `skills/project-handoff-doc-stack/SKILL.md` | Durable root handoff docs and sync rules for multi-doc project stacks |
 | `ai validate knowledge` | `skills/claude-meta/validate-knowledge/skill.md` | Lints skills and knowledge against the hybrid format protocol |
+| `ai validate knowledge fast` | `codex-router/Validate-CodexKnowledge.ps1` | Read-only fast validation for routes, duplicate names, local target paths, secret-like patterns, and oversized files |
+| `ai trigger conflict` | `codex-router/Audit-CodexRouting.ps1` | Detect exact trigger phrases that map to different destinations |
+| `ai route telemetry` | `codex-router/Measure-CodexRouteTelemetry.ps1` | Measure route bytes and estimated tokens for Luna Medium efficiency |
 | `ai router blueprint` | `memories/0_apex/templates/MASTER_ROUTER_BLUEPRINT.md` | Lightweight multi-project root router blueprint |
 | `create module` / `new module` | `skills/claude/create-module/skill.md` | End-to-end CRUD module generator - SQL, types, store, views, routes, i18n, workflow tests |
 | `generate views` | `skills/claude/generate-views/skill.md` | Vue component suite for list, detail, form, and drawer variants |
diff --git a/2_governance/bridges/ai_studio_bridge.md b/2_governance/bridges/ai_studio_bridge.md
--- a/2_governance/bridges/ai_studio_bridge.md
+++ b/2_governance/bridges/ai_studio_bridge.md
@@ -3,7 +3,7 @@
 description: "Google AI Studio Vision Bridge (Vision Pulse)"
 triggers: ["ai studio bridge", "ai_studio_bridge", "google studio vision"]
 phase: reference
-model_hint: codex-gpt-5.3
+model_hint: medium
 version: 1.0
 _ohdy_wrapper: |-
   # OHDY COMPRESSED NODE (V42.0)
diff --git a/2_governance/bridges/interactive_ui_bridge.md b/2_governance/bridges/interactive_ui_bridge.md
--- a/2_governance/bridges/interactive_ui_bridge.md
+++ b/2_governance/bridges/interactive_ui_bridge.md
@@ -3,7 +3,7 @@
 description: "Interactive UI Hyper-Protocol"
 triggers: ["interactive ui bridge", "interactive_ui_bridge", "interactive hyper"]
 phase: reference
-model_hint: codex-gpt-5.3
+model_hint: medium
 version: 1.0
 _ohdy_wrapper: |-
   # OHDY COMPRESSED NODE (V42.0)
diff --git a/2_governance/bridges/stitch_bridge.md b/2_governance/bridges/stitch_bridge.md
--- a/2_governance/bridges/stitch_bridge.md
+++ b/2_governance/bridges/stitch_bridge.md
@@ -3,7 +3,7 @@
 description: "Google Stitch Logic Bridge"
 triggers: ["stitch bridge", "stitch_bridge", "google stitch logic"]
 phase: reference
-model_hint: codex-gpt-5.3
+model_hint: medium
 version: 1.0
 _ohdy_wrapper: |-
   # OHDY COMPRESSED NODE (V42.0)
diff --git a/2_governance/experience_vault.md b/2_governance/experience_vault.md
--- a/2_governance/experience_vault.md
+++ b/2_governance/experience_vault.md
@@ -3,7 +3,7 @@
 description: "Sovereign Mission Ledger & Logic Breakthroughs (V15.1 Apex)"
 triggers: ["experience vault", "mission log", "neural vectors", "breakthroughs"]
 phase: governance
-model_hint: codex-gpt-5.3
+model_hint: medium
 version: 15.1
 status: authoritative
 ---
diff --git a/CLAUDE_BLUEPRINT_RECIPE.md b/CLAUDE_BLUEPRINT_RECIPE.md
--- a/CLAUDE_BLUEPRINT_RECIPE.md
+++ b/CLAUDE_BLUEPRINT_RECIPE.md
@@ -5,8 +5,8 @@
 tier: 2
 phase: 1-execution
 priority: HIGH
-model_hint: codex-gpt-5.3
-applies_to: ["claude", "claude-code", "codex-gpt-5.3", "antigravity"]
+model_hint: medium
+applies_to: ["claude", "claude-code", "luna-5.6-medium", "antigravity"]
 requires: ["0_apex/SOVEREIGN_BLUEPRINT_PROTOCOL.md", "2_governance/SOVEREIGN_BLUEPRINT_PROCEDURE.md", "0_apex/templates/MASTER_BLUEPRINT.md", "0_apex/templates/MASTER_APP_BLUEPRINT.md", "0_apex/templates/MASTER_DESIGN.md", "0_apex/templates/MASTER_ROUTER_BLUEPRINT.md", "0_apex/templates/BLUEPRINT_SAMPLES.md"]
 unlocks: ["MOBILE_APP_DESIGN_RECIPE.md", "IMAGE_TO_MOBILE_APP_PIPELINE.md"]
 related: ["0_apex/USER_DNA.md", "1_core/DESIGN_SOP.md", "1_core/UI_DNA_MASTER.md", "archive/wrider_design_senses.md"]
diff --git a/IMAGE_TO_MOBILE_APP_PIPELINE.md b/IMAGE_TO_MOBILE_APP_PIPELINE.md
--- a/IMAGE_TO_MOBILE_APP_PIPELINE.md
+++ b/IMAGE_TO_MOBILE_APP_PIPELINE.md
@@ -5,8 +5,8 @@
 tier: 2
 phase: 1-execution
 priority: HIGH
-model_hint: codex-gpt-5.3
-applies_to: ["claude", "claude-code", "codex-gpt-5.3", "antigravity"]
+model_hint: medium
+applies_to: ["claude", "claude-code", "luna-5.6-medium", "antigravity"]
 authored_by: claude-opus-4-7
 authored_for: "shared cross-AI use"
 requires: ["MOBILE_APP_DESIGN_RECIPE.md", "CLAUDE_BLUEPRINT_RECIPE.md", "0_apex/USER_DNA.md", "1_core/DESIGN_SOP.md", "0_apex/SOVEREIGN_BLUEPRINT_PROTOCOL.md"]
diff --git a/MOBILE_APP_DESIGN_RECIPE.md b/MOBILE_APP_DESIGN_RECIPE.md
--- a/MOBILE_APP_DESIGN_RECIPE.md
+++ b/MOBILE_APP_DESIGN_RECIPE.md
@@ -1,12 +1,12 @@
-﻿---
+---
 name: mobile-app-design-recipe
 description: "Step-by-step build guide for the user's preferred mobile app aesthetic (fintech / utility, 412 px viewport, mint+gold+ink, DM Sans, 23-view shape with toast system + animated FAB + Design Kit). Use as the canonical clone-template when the user asks for a new mobile app, sample app, dashboard prototype, or 'build me an app like the wallet template'."
 type: procedure
 tier: 2
 phase: 1-execution
 priority: HIGH
-model_hint: codex-gpt-5.3
-applies_to: ["claude", "claude-code", "codex-gpt-5.3", "antigravity"]
+model_hint: medium
+applies_to: ["claude", "claude-code", "luna-5.6-medium", "antigravity"]
 authored_by: claude-opus-4-7
 authored_for: "shared cross-AI use"
 requires: ["CLAUDE_BLUEPRINT_RECIPE.md", "0_apex/USER_DNA.md", "1_core/DESIGN_SOP.md", "0_apex/SOVEREIGN_BLUEPRINT_PROTOCOL.md"]
diff --git a/extensions/ad_hoc/notes/2026-07-07T00-00-00-cold-storage-promotion-policy.md b/extensions/ad_hoc/notes/2026-07-07T00-00-00-cold-storage-promotion-policy.md
--- a/extensions/ad_hoc/notes/2026-07-07T00-00-00-cold-storage-promotion-policy.md
+++ b/extensions/ad_hoc/notes/2026-07-07T00-00-00-cold-storage-promotion-policy.md
@@ -45,6 +45,12 @@
 - If the file is a reusable principle, convert it into a short note with hybrid frontmatter.
 - If the file is a cleanup candidate only, leave it cold and reference it from memory summaries instead.
 
+## Cold means deferred, not deleted
+- Excluded cold notes remain on disk and are not trash.
+- When a task is semantically related, search cold filenames, frontmatter, and body text before concluding that knowledge is missing.
+- Read the matching cold note on demand, then promote only the reusable rule or route that improves future decisions.
+- Promotion can happen later when repeated work proves the note is worth a hot trigger; cold status is reversible.
+
 ## Default decision
 - Keep the hot path lean.
 - Promote only the best reusable knowledge.
diff --git a/extensions/ad_hoc/notes/2026-07-10-gpt-5-6-luna-operating-playbook.md b/extensions/ad_hoc/notes/2026-07-10-gpt-5-6-luna-operating-playbook.md
new file mode 100644
--- /dev/null
+++ b/extensions/ad_hoc/notes/2026-07-10-gpt-5-6-luna-operating-playbook.md
@@ -0,0 +1,71 @@
+---
+name: gpt-5-6-luna-operating-playbook
+description: "General GPT-5.6 Luna operating rules for high-context, tool-using, cache-aware Codex work."
+triggers: ["__auto_after_codex_boot__", "__luna_context__"]
+activation: task_state_deep
+read_mode: deep_only
+keep_awake: same_lane
+unload_when: lane_change
+priority: warm
+canonical_for: [model_optimization, knowledge_review, routing_validation]
+status: authoritative
+phase: execution
+model_hint: medium
+model_profile: luna-5.6-medium
+version: 1.0
+date_updated: "2026-07-10"
+source:
+  - "https://docsbot.ai/models/compare/gpt-5-6-luna/gpt-5-4-mini"
+related_docs:
+  - memories/2_governance/MODEL_COST_OPTIMIZATION_POLICY.md
+  - memories/extensions/ad_hoc/notes/2026-07-10-luna-5-6-vue-vben-execution-profile.md
+  - skills/claude-meta/validate-knowledge/skill.md
+---
+
+# GPT-5.6 Luna operating playbook
+
+This is the general Luna layer for deep model-optimization, knowledge review, routing validation, and explicit `.codex` improvement work. Project-specific files, current source, tests, and explicit user instructions remain higher-signal than this note.
+
+Knowledge labels used in this file:
+
+- `RULE`: an operational behavior to follow.
+- `PREFERENCE`: a user or project taste/default that may be overridden by current project truth.
+- `EVIDENCE`: a historical or external observation that must not become an absolute rule without verification.
+
+`EVIDENCE:` The linked DocsBot comparison reports a 400K context window, 128K maximum output, predictable prompt caching across the GPT-5.6 family, a fast/affordable high-volume tier, and 82.5% on Terminal-Bench 2.1 under its listed settings. Treat provider-facing model facts and benchmark numbers as article-reported until verified against current official OpenAI documentation; use them to shape workflow, not to bypass evidence.
+
+## Top 10 advantages to operationalize
+
+1. **RULE — Large context is a retrieval advantage, not permission to dump the tree.** Load the smallest relevant route first, then expand in bounded batches. Use the larger window for connected evidence, not repeated or unrelated files.
+2. **RULE — Long output is an artifact budget.** Use normal concise replies; reserve large completions for complete source files, migrations, generated docs, or test artifacts that genuinely need them.
+3. **RULE — Fast, affordable throughput favors Lean Lane by default.** Finish routine work with narrow reads, short updates, and one nearest verification target before escalating.
+4. **RULE — Prompt-cache stability is a design constraint.** Keep durable instructions and stable route prefixes unchanged; place volatile task details after them. Avoid needless edits to hot boot files.
+5. **RULE — Tool use should be an inspect-act-verify loop.** For coding tasks, gather current file evidence, make the smallest patch, read it back, and run the nearest useful check.
+6. **Use parallelism for independent evidence.** Batch unrelated file reads, searches, and checks together; keep dependent operations sequential so the evidence chain stays clear.
+7. **Spend deeper reasoning on ambiguity and risk.** Start with low-cost hypotheses and escalate only for unknown root causes, architecture, security, governance, or failed verification.
+8. **Exploit terminal strength through executable acceptance criteria.** Prefer `rg`, focused scripts, type checks, builds, smoke requests, and browser checks over prose claims that code is correct.
+9. **Keep model uncertainty visible.** A comparison page may contain stale, incomplete, or vendor-generated data; label article-derived claims, verify unstable facts, and never invent missing capabilities, prices, or access entitlements.
+10. **Measure the workflow, not only the model.** For repeated routes, track prompt/completion tokens when available, cache reuse, wall-clock time, route correctness, verification result, and whether the extra context changed the outcome.
+
+## Default Luna execution contract
+
+`HYDRATE route -> GROUND current evidence -> PLAN smallest change -> ACT -> VERIFY -> REPORT outcome plus verification.`
+
+- Keep the user’s fixed constraints and exact error text verbatim.
+- Keep one matching skill family awake while the task remains in the same lane.
+- Prefer one source of truth and one verification target per task.
+- Do not confuse a large context window with permission to read secrets, sessions, unrelated history, or the full `.codex` tree.
+- If three fixes fail on the same issue, stop repeating the same tactic and switch to a deeper audit.
+
+## Activation
+
+This note is not a user-facing trigger. PULSE supplies the compact Luna digest during boot. Load this full playbook only when the task enters deep model-optimization, knowledge-review, routing-validation, or explicit `.codex` improvement work. Keep it awake while that deep lane remains active; unload it when the lane changes.
+
+Task-specific routes still take precedence when a project or skill requires deeper instructions, such as the separate Vue/Vben Luna profile.
+
+## What this changes in `.codex`
+
+- General Luna behavior lives here instead of bloating `00_PULSE.md`.
+- `ai luna 5.6` remains the only user-facing Luna-specific route, for Vue/Vben work.
+- The cost policy uses Luna as the active baseline while retaining portable Lean/Balanced/Deep lanes.
+- Existing project-specific skills remain authoritative for their own workflows.
diff --git a/extensions/ad_hoc/notes/2026-07-10-localhost-test-trigger.md b/extensions/ad_hoc/notes/2026-07-10-localhost-test-trigger.md
new file mode 100644
--- /dev/null
+++ b/extensions/ad_hoc/notes/2026-07-10-localhost-test-trigger.md
@@ -0,0 +1,68 @@
+---
+name: localhost-test-trigger
+description: "Fast local development startup and browser verification for mixed project roots. Detects real Vben admin, Vue app, and PHP website projects; skips templates and reference folders."
+triggers: ["localhost test"]
+status: authoritative
+date_updated: "2026-07-10"
+---
+
+# `localhost test` trigger
+
+When the user says exactly `localhost test`, inspect the current workspace root first and automatically detect runnable project roots. Do not assume every sibling folder is an active product.
+
+## Detection and classification
+
+1. Use the current working directory as the workspace root.
+2. Inspect only shallow candidate folders first; exclude `node_modules`, `dist`, `build`, `.git`, test-result folders, generated output, and nested dependency copies.
+3. Classify a folder as a Vben admin when it has a pnpm monorepo/package workspace plus an active `apps/web-antd` app and a `dev:local` script. The admin command is `pnpm.cmd run dev:local` from the monorepo root.
+4. Classify a folder as a Vue app when it has its own `package.json`, Vite/Vue source, and a `dev` script, but is not the Vben monorepo. The default command is `npm.cmd run dev` from that app root.
+5. Classify a folder as a PHP website when it has PHP entry files such as `index.php`, `router.php`, or `composer.json`. Run it from the folder containing the real front controller.
+6. Treat folders with only documentation, screenshots, fixtures, examples, archived code, or reference/template markers as non-runnable and report them as skipped with the reason.
+7. If a project has an explicit `PROJECT_CONTEXT.md`, `STATUS.md`, `README.md`, or local env file, read the smallest relevant one to confirm its role and port before starting it.
+
+## Current workspace sample
+
+For `trash-container-app`:
+
+| Folder | Type | Command | Current port |
+|---|---|---|---:|
+| `admin-panel-trash` | Vben admin | `pnpm.cmd run dev:local` | `6006` |
+| `web-admin-app` | Vue app | `npm.cmd run dev` | `5173` |
+| `web-driver-app` | Vue app | `npm.cmd run dev` | `3000` |
+
+There is no PHP website in this workspace, so website startup is skipped.
+
+## Startup and verification rules
+
+- Check whether the expected port is already listening before launching a server; reuse and verify an existing healthy server instead of starting duplicates.
+- Prefer the project’s configured `VITE_PORT`; if absent, read the Vite config, then choose the first free port in the project’s safe range.
+- On Windows use `npm.cmd` and `pnpm.cmd`.
+- Start dev servers detached/background so they survive the command turn.
+- For PHP, bind IPv4 explicitly with `php -S 127.0.0.1:<port> <front-controller>` and choose the first free port from `8000` through `8003`.
+- Wait for the server-ready signal, then verify each URL with an HTTP request. A process start alone is not success.
+- For a `500`, inspect the server output/log as an application error. For `000` or connection refused, inspect process/port ownership and restart safely.
+- Do not change source code, env secrets, database state, or project configuration as part of a normal localhost test unless the user separately asks for a fix.
+
+## Final response format
+
+Report:
+
+1. detected active projects grouped by type
+2. skipped folders and why
+3. each verified browser URL as a clickable link
+4. HTTP result and server state
+5. any blocked project with the exact error and the smallest next action
+
+When a server is verified, always show its URL again as a standalone plain URL on its own line, without requiring the user to extract it from a status sentence.
+
+Use one standalone URL per line for multiple projects:
+
+http://127.0.0.1:<port>/
+
+Keep project labels and HTTP status separately, but never omit this raw URL block.
+
+For this sample, the expected browser links are:
+
+- `http://127.0.0.1:6006/` — desktop Vben admin
+- `http://127.0.0.1:5173/` — mobile admin Vue app
+- `http://127.0.0.1:3000/` — driver Vue app
diff --git a/extensions/ad_hoc/notes/2026-07-10-luna-5-6-vue-vben-execution-profile.md b/extensions/ad_hoc/notes/2026-07-10-luna-5-6-vue-vben-execution-profile.md
new file mode 100644
--- /dev/null
+++ b/extensions/ad_hoc/notes/2026-07-10-luna-5-6-vue-vben-execution-profile.md
@@ -0,0 +1,55 @@
+---
+name: luna-5-6-vue-vben-execution-profile
+description: "Luna 5.6 execution profile for Vue 3 Composition API, strict TypeScript, Ant Design Vue, and Vben Admin work. Converts the user's coding preferences into enforceable implementation and verification gates."
+triggers: ["ai luna 5.6", "GPT-5.6 Luna", "Vue 3 Vben rules", "strict Vben form rules"]
+status: authoritative
+date_updated: "2026-07-10"
+related_docs:
+  - skills/claude/README.md
+  - skills/claude/VBEN_ADMIN_MANDATORY_CHECKLIST.md
+  - skills/claude/generate-views/skill.md
+  - memories/extensions/ad_hoc/notes/2026-07-03T00-00-01-merged-codex-performance-layer.md
+---
+
+# Luna 5.6 Vue/Vben execution profile
+
+Use this profile as a compact enforcement layer. Existing project files and the Vben checklist remain the source of truth when a project-specific convention differs.
+
+## Implementation rules
+
+1. Use Vue 3 `<script setup lang="ts">` and Composition API patterns.
+2. Use strict TypeScript at boundaries. Explicitly map database `snake_case` to frontend/store/form `camelCase`; do not hide drift with `any` or broad casts.
+3. In Vben forms, use the native `useEntityForm` structure and declare schemas as `const formSchema: FormSchema[] = [...]`.
+4. If a form option or schema value depends on reactive store data, use `computed<FormSchema[]>(...)` while preserving the same typed schema contract. Do not freeze runtime relation options in a static array.
+5. Keep dynamic/non-scalar state outside `useEntityForm` when the project pattern requires it, such as uploads, rich text, or repeatable arrays; map it explicitly into the submit payload and reset path.
+6. Use Ant Design Vue/Vben components and existing project patterns. Do not replace established components with rough native controls without a deliberate project reason.
+7. All user-visible labels, placeholders, messages, titles, empty states, and action text must use `$t('...')` and have matching keys in every required locale file. Never leave literal English or Chinese UI copy in templates.
+8. Output complete code structures. Never use placeholders such as `// rest of the code`, omit closing blocks, or truncate a file in a way that cannot compile.
+
+## Luna execution loop
+
+1. Read the smallest matching project truth document and skill front door.
+2. Preserve the user's fixed constraints verbatim before editing.
+3. Inspect the current file and nearby pattern before choosing a code shape.
+4. Make the smallest surgical change.
+5. Read back the edited region and verify structural closure through the compiler, not visual counting alone.
+6. Run the nearest useful check: `vue-tsc --noEmit`, project type-check, lint, build, or a browser smoke test.
+7. Report the exact verification result and any blocker; never claim structural correctness from an edit alone.
+
+## Form acceptance gates
+
+- `FormSchema[]` is explicitly typed.
+- Labels/placeholders/messages use translation keys.
+- Reactive relation/select options are computed and refreshed after create/update when required.
+- `useEntityForm` owns scalar form state; separate dynamic state is explicitly synchronized.
+- Submit payload has an explicit DB-boundary mapping and no accidental UI-only fields.
+- Reset/close clears form state, single-flight locks, uploads, and dynamic state according to the active project pattern.
+- Create/edit flows retain the anti-double-submit and drawer-reset protections from the mandatory Vben checklist.
+
+## What this profile improves
+
+- It turns “preserve localization” into a locale-parity gate.
+- It turns “use standard form schema” into a typed, reactive schema rule.
+- It turns “validate closing tags” into compiler/read-back verification.
+- It preserves Luna's concise output preference while making verification stricter, not weaker.
+
diff --git a/extensions/ad_hoc/notes/2026-07-10-pinia-c-to-c-quality-principles.md b/extensions/ad_hoc/notes/2026-07-10-pinia-c-to-c-quality-principles.md
new file mode 100644
--- /dev/null
+++ b/extensions/ad_hoc/notes/2026-07-10-pinia-c-to-c-quality-principles.md
@@ -0,0 +1,7 @@
+# Pinia and c-to-c Quality Principles
+
+Use these as high-priority implementation rules for the user's Pinia stores and reusable c-to-c functions. Keep the public `PiniaStore`, `Function`, and `Input` cards unchanged while improving the inside: pure small functions, Pinia orchestration, typed Input/Result contracts, request deduplication, separate read/write pipelines, retry-safe CRUD mutations, centralized errors, store-boundary database translation, selectors, and tests from small c upward.
+
+CRUD is a good internal model when the domain supports create/read/update/delete, but the spreadsheet's exact action names always win (`getDetail`, `getLists`, `getAllByDate`, and similar names must not be silently renamed). Add short comments at store, action, and non-obvious c-to-c boundaries describing purpose, Input/Output, side effects, and composition order so humans and AI can trace behavior deeper without reading noise.
+
+For performance, deduplicate in-flight requests before adding caches, invalidate only affected state after writes, and avoid caching stale or permission-sensitive data. Verify with exact-name search, c-to-c unit tests, Pinia orchestration tests, TypeScript checks, builds, and one visible caller smoke test.
diff --git a/extensions/ad_hoc/notes/2026-07-10-pinia-store-contract-reference.md b/extensions/ad_hoc/notes/2026-07-10-pinia-store-contract-reference.md
new file mode 100644
--- /dev/null
+++ b/extensions/ad_hoc/notes/2026-07-10-pinia-store-contract-reference.md
@@ -0,0 +1,54 @@
+---
+name: pinia-store-contract-reference
+description: "User-owned Pinia contract rule and hotpot reference paths for app store naming and page usage."
+triggers: ["PiniaStore Function Input", "useOrderStore", "Pinia contract", "Google Sheet Pinia"]
+status: authoritative
+date_updated: "2026-07-10"
+---
+
+# Pinia contract reference
+
+For app work, the user's Google Sheet `Trash`, tab `Pinia`, defines a public contract. Preserve the exact spelling and casing of the columns and values:
+
+`PiniaStore` → `Function` → `Input`
+
+These are not loose documentation labels. They are the names workmates use to find and call store actions. Do not rename, alias, or silently change them during implementation.
+
+Reference projects to inspect before designing an app store:
+
+- `D:\project\hotpot\app-cashier-hotpot`
+- `D:\project\hotpot\admin-panel-hotpot`
+- `D:\project\hotpot\webApp-hotpot`
+
+Observed reference shape:
+
+- stores use `defineStore('<id>', { state: () => ({}), actions: { ... } })`
+- pages instantiate stores with `const merchantStore = useMerchantStore()` or the equivalent exact store name
+- pages call actions from `onMounted`/fetch functions and assign typed results to local refs
+- independent actions are often loaded with `Promise.all`
+- `authStore` controls role/member-dependent calls and Inputs
+- API calls stay inside Pinia actions
+
+The exact `useOrderStore` implementation was not found in the three inspected hotpot roots; `useOrderStore` and its action names come from the user's `Pinia` sheet. Use the hotpot stores such as `useMerchantStore`, `useMemberStore`, and auth-dependent page loading as the structural reference, not as a source for renaming the user's contract.
+
+When editing an app store, change only the requested store implementation area unless the user explicitly expands scope. Search callers, preserve exact sheet Inputs, and run TypeScript verification after editing.
+
+## High-priority `c-to-c` composition rule
+
+Use the user's `c-to-c` model for all Vue app work:
+
+- `c` = one very small, single-purpose function
+- `c + c` or `c + c + c` = a reusable utility composition
+- utilities in `utils/` and types in `types/` feed store actions
+- Pinia actions are the largest page-facing compositions and keep the exact sheet contract
+- pages call `useStore().Function(Input)` and do not duplicate the internal chain
+
+Preferred dependency direction:
+
+`views -> stores -> utils/types`
+
+Pass auth and other state into small functions as explicit Input. Do not make utility functions import views or secretly mutate stores. Search callers before changing a small `c`, and verify the largest composed store action after changes.
+
+## Vben Admin extension
+
+The same method applies to Vben Admin list/detail/form/drawer modules: `views/tables/drawers -> Pinia store action -> c-to-c utils/types -> API/Supabase`. Keep `useEntityForm` and Vben drawer lifecycle behavior in the view/composable layer, but keep query, permission, normalization, persistence, and refresh composition in the store boundary. Preserve exact spreadsheet action names and Inputs, use `$t()` for all Vben UI text, and retain cloned module shells when the data flow is reused.
diff --git a/extensions/ad_hoc/notes/2026-07-10-pinia-store-relation-binding.md b/extensions/ad_hoc/notes/2026-07-10-pinia-store-relation-binding.md
new file mode 100644
--- /dev/null
+++ b/extensions/ad_hoc/notes/2026-07-10-pinia-store-relation-binding.md
@@ -0,0 +1,5 @@
+# Pinia Store Relation Binding
+
+Treat each Pinia store as a bound reusable package: store implementation, `src/types/<store>.ts`, `src/utils/*-store.ts` c-to-c helpers, barrel exports, view/table/drawer callers, composables, tests, mocks, route loaders, and documentation. Prefer direct store-specific type imports while keeping shared entities in `src/types/entities.ts`.
+
+For an explicit store-removal request, Codex should inventory actual imports and callers, remove the implementation plus unused private types/helpers/tests/callers/barrel exports together, and retain shared files still used elsewhere. Never delete by filename alone. Finish with stale-import search, type-check, build, and a visible smoke test.
diff --git a/extensions/ad_hoc/notes/2026-07-10-portable-pinia-store-package.md b/extensions/ad_hoc/notes/2026-07-10-portable-pinia-store-package.md
new file mode 100644
--- /dev/null
+++ b/extensions/ad_hoc/notes/2026-07-10-portable-pinia-store-package.md
@@ -0,0 +1,7 @@
+# Portable Pinia Store Package Rule
+
+The completed `web-driver-app` Pinia implementation is a reusable reference package for future apps in the same product/domain family. When another app has compatible tables, auth context, API/Supabase boundaries, and TypeScript models, Codex may copy the `stores/`, related `utils/`, and `types/` implementation as a starting point instead of rebuilding the Pinia layer from zero.
+
+Keep the public cards stable: exact `PiniaStore`, `Function`, and `Input` names remain the page-facing contract. Reuse is allowed inside the implementation, but Codex must compare the target app's schema, environment variables, auth roles, response shapes, route Inputs, and existing callers before copying. Adapt only incompatible internals and preserve the c-to-c direction: `views/tables/drawers -> stores -> utils/types -> API/Supabase`.
+
+When a same-project app needs the same behavior, prefer copying the complete store package together with its c-to-c helpers and types, then run exact-name search, TypeScript check, build, and a visible page smoke test. Do not copy blindly across unrelated schemas or silently rename contract actions.
diff --git a/project_notes/ANGEL_INTERIOR_LOCAL_DEV.md b/project_notes/ANGEL_INTERIOR_LOCAL_DEV.md
--- a/project_notes/ANGEL_INTERIOR_LOCAL_DEV.md
+++ b/project_notes/ANGEL_INTERIOR_LOCAL_DEV.md
@@ -1,5 +1,5 @@
 ---
-name: angel-interior-local-dev
+name: angel-interior-local-dev-memory
 description: "Angel Interior localhost startup — exact working commands for PHP website and Vben admin panel. run-local.bat silently exits; use direct PHP command instead."
 metadata:
   type: project
diff --git a/project_notes/VBEN_RELATION_AUTOGUARD_PLAYBOOK.md b/project_notes/VBEN_RELATION_AUTOGUARD_PLAYBOOK.md
--- a/project_notes/VBEN_RELATION_AUTOGUARD_PLAYBOOK.md
+++ b/project_notes/VBEN_RELATION_AUTOGUARD_PLAYBOOK.md
@@ -1,9 +1,9 @@
-﻿---
-name: vben-relation-autoguard-playbook
+---
+name: vben-relation-autoguard-memory
 description: "Unified auto-detection and auto-fix workflow for relation visibility, relation navigation, and destructive-delete safety in vben + Supabase admin apps."
 triggers: ["vben relation autoguard", "auto detect relation issue", "parent child auto fix", "typed delete relation"]
 phase: orchestrator
-model_hint: gpt-5.3-codex
+model_hint: medium
 version: 1.1
 status: active
 date_created: "2026-05-12"
diff --git a/raw_memories.md b/raw_memories.md
--- a/raw_memories.md
+++ b/raw_memories.md
@@ -2,571 +2,6 @@
 
 Merged stage-1 raw memories (stable ascending thread-id order):
 
-## Thread `019eda34-cf7c-7a73-a898-44036512f0ac`
-updated_at: 2026-06-18T10:11:50+00:00
-cwd: \\?\C:\xampp\htdocs
-rollout_path: C:\Users\user\.codex\sessions\2026\06\18\rollout-2026-06-18T18-09-23-019eda34-cf7c-7a73-a898-44036512f0ac.jsonl
-rollout_summary_file: 2026-06-18T10-09-14-L9so-kushiro_api_domain_swap_localhost_preserved.md
-
----
-description: Updated Kushiro PHP API runtime domains from old kushirobbq hostnames to kushiro.my while preserving localhost dev behavior; verified with php -l and search sweep.
-task: update-api-domain-routing-and-upload-endpoints
-task_group: php-api/domain-config
- task_outcome: success
-cwd: C:\xampp\htdocs
-keywords: KushiroBBQ, Config.php, Helper.php, upload_receiver.php, RedirectController.php, APP_ENV, localhost, kushiro.my, domain swap, CORS, upload URL, php -l, rg
----
-
-### Task 1: Update Kushiro API domain routing and upload endpoints
-
-task: update Kushiro PHP API staging/production domains from old kushirobbq hostnames to kushiro.my while keeping localhost dev intact
-task_group: php-api/domain-config
-task_outcome: success
-
-Preference signals:
-- user said: "dev is ok.. localhost.. but publish or staging are using kushiro.my this is my official website" -> keep localhost behavior unchanged and only swap published/staging hostnames.
-- user clarified: "for any kushirobbq.com.my better to help me update to kushiro.my" -> when working in this Kushiro workspace, treat the old public domain as a bulk replacement target.
-
-Reusable knowledge:
-- `api/KushiroBBQ/Config.php` holds the environment-specific URL/origin settings; `APP_ENV` is read from `api/KushiroBBQ/.env` in `Helper.php`.
-- The important domain touchpoints were: `allowedOrigins`, `cookieDomain`, `domain`, `attachmentUrl`, `VPS_UPLOAD_URL`, `VPS_DOMAIN`, `VPS_SERVER_NAME`, host checks in `getCorporateIdByHost()`, `upload_receiver.php` public URL/path, and the default redirect link.
-- `php -l` on edited PHP files is a quick verification step that caught no syntax issues here.
-
-Failures and how to do differently:
-- The old domain was scattered across multiple files, so a workspace-wide `rg` sweep was necessary before patching.
-- The upload receiver path was changed to `/home/zetagrp/public_html/kushiro.my/uploads/images/`; if the real deployment folder differs, the server-side path needs a follow-up fix.
-
-References:
-- Search hits before patching included:
-  - `api/KushiroBBQ/Config.php:85-99` old production/staging URLs
-  - `api/KushiroBBQ/Helper.php:42-46` old upload URL/domain constants
-  - `api/KushiroBBQ/Helper.php:974-978` host matching for `kushirobbq.com.my` / `admin.kushirobbq.com.my`
-  - `api/upload_receiver.php:4,85,130` old upload receiver URL/path/returned URL
-  - `api/KushiroBBQ/Controllers/RedirectController.php:78` old default link
-- Validation outputs:
-  - `No syntax errors detected in api\KushiroBBQ\Config.php`
-  - `No syntax errors detected in api\KushiroBBQ\Helper.php`
-  - `No syntax errors detected in api\upload_receiver.php`
-  - `No syntax errors detected in api\KushiroBBQ\Controllers\RedirectController.php`
-- Final `rg` search over the workspace returned exit code 1 with no remaining live runtime matches for `kushirobbq.com.my`, `kushirobbq.zetacapital.my`, `admin-kushirobbq.zetacapital.my`, `admin.kushirobbq.com.my`, or `api.kushirobbq.com.my`.
-
-## Thread `019ef211-9b91-7493-8298-29bbba8d21d1`
-updated_at: 2026-06-23T03:55:01+00:00
-cwd: \\?\C:\Users\user\.codex
-rollout_path: C:\Users\user\.codex\sessions\2026\06\23\rollout-2026-06-23T09-21-47-019ef211-9b91-7493-8298-29bbba8d21d1.jsonl
-rollout_summary_file: 2026-06-23T01-21-40-OmVb-codex_cleanup_routing_checkpoint_skill_frontdoors.md
-
-description: `.codex` cleanup and routing hardening rollout; user wanted to remove duplicate/legacy history, convert broad skills to README-first front doors, verify routing integrity, and then rate the system / suggest the next highest-leverage improvement. Outcome was mostly success, with one temporary over-trim of MEMORY.md that was restored from git and a real routing break fixed by re-adding PREFLIGHT_CHECKLIST.md and regenerating the router snapshot.
-task: `.codex` knowledge/skills cleanup, routing verification, and rating review
-task_group: `.codex` routing / memory hygiene
-task_outcome: success
-cwd: C:\Users\user\.codex
-keywords: .codex, routing, audit, Update-CodexRouting.ps1, Audit-CodexRouting.ps1, 00_PULSE.md, AGENTS.md, CODEX_DYNAMIC_ROUTING.md, PREFLIGHT_CHECKLIST.md, README-first, starting-point, claude, memory cleanup, legacy history, safe trim, git show HEAD, token cost, speed, rating, comparison tables
----
-
-### Task 1: Reduce duplicate `.codex` boot/routing prose
-
-task: shrink duplicate boot / routing content in `00_CODEX_START_HERE.md`, `AGENTS.md`, `CODEX_DYNAMIC_ROUTING.md`, and `memories/MEMORY.md`
-task_group: `.codex` boot/routing hygiene
-task_outcome: success
-
-Preference signals:
-- when the user asked to find “10 parts that are a waste or unnecessary content” in `.codex` knowledge/memories/skills, they want the boot path to stay lean and duplicate wording removed by default.
-- when the user asked for comparison tables and a 1/10 rating, they want before/after scoring, not just edits.
-- when the user repeatedly confirmed each step, they were comfortable with incremental cleanup as long as each step was explained and verified.
-
-Reusable knowledge:
-- `00_PULSE.md` is the authoritative single boot read for `.codex` routing.
-- `00_CODEX_START_HERE.md` should remain deferred and short.
-- `AGENTS.md` should be a compact bridge, not a second boot manual.
-- `CODEX_DYNAMIC_ROUTING.md` should act like a snapshot/index, not a long change log.
-
-Failures and how to do differently:
-- Patch matching against `memories/MEMORY.md` can fail when punctuation/encoding differs; use smaller replacements and read back immediately.
-- Over-trimming `memories/MEMORY.md` can corrupt structure; if that happens, restore from git before trying again.
-
-References:
-- `00_CODEX_START_HERE.md` reduced to `1407` bytes.
-- `AGENTS.md` reduced to `2913` bytes.
-- `CODEX_DYNAMIC_ROUTING.md` compressed to a fallback snapshot.
-- `memories/MEMORY.md` top section reduced to a two-line pointer, then later restored after an over-trim.
-
-### Task 2: Convert broad skills to README-first front doors
-
-task: add `README.md` front doors for broad skills and route them from PULSE / skill router
-task_group: skill routing / front-door cleanup
-task_outcome: success
-
-Preference signals:
-- when the user asked whether other skills could use `README.md`, they wanted the same lean front-door pattern repeated where it saves context.
-- when they confirmed the `starting-point` choice, they were willing to apply the pattern to other broad skills.
-
-Reusable knowledge:
-- Broad bootstrap skills benefit most from README-first routing because they are common entry points and can be expensive if the executor loads too early.
-- `starting-point` is the right sort of broad bootstrap skill to convert before narrower / always-active skills.
-
-Failures and how to do differently:
-- README files must stay tiny; they should point to deeper work, not copy it.
-- Do not put detailed workflow back into the front door after converting it.
-
-References:
-- `00_PULSE.md` now routes `ai starting point` to `skills/starting-point/README.md`.
-- `memories/2_governance/artifacts/skill_path_router.md` updated to match.
-- `skills/starting-point/README.md` created as a short front door.
-- `skills/starting-point/skill.md` now points back to the README.
-
-### Task 3: Deep clean old `.codex` history and empty folders
-
-task: delete safe historical content older than one week and remove empty directories
-task_group: `.codex` history cleanup
-task_outcome: success
-
-Preference signals:
-- when the user asked to remove old history / legacy content and then explicitly asked to remove empty folders, they want aggressive cleanup of obvious archival noise.
-- when they worried the trim was “too much,” they want conservative, stepwise cleanup rather than blind deletion.
-
-Reusable knowledge:
-- `memories/archive` and old `sessions` / `.sandbox` history were safe cleanup targets in this session.
-- `memories/rollout_summaries` had no files older than a week, so it was preserved.
-
-Failures and how to do differently:
-- The cleanup process can accidentally over-trim files if not checkpointed.
-- Avoid treating every old-looking file as disposable; some are still routing-critical.
-
-References:
-- `memories/archive` removed.
-- Old `sessions` files older than 1 week removed.
-- Old `.sandbox` log files older than 1 week removed.
-- Empty folders pruned.
-
-### Task 4: Repair routing after cleanup and confirm the router still works
-
-task: fix the missing mandatory route target and re-validate the router
-task_group: routing repair / verification
-task_outcome: success
-
-Preference signals:
-- when the user asked to make sure all router routing works after cleanup, they want verification as part of the fix.
-- when they asked “if anything wrong on routing ai please help me fix,” they want concrete repair of broken routing, not just a report.
-
-Reusable knowledge:
-- `memories/2_governance/PREFLIGHT_CHECKLIST.md` is a required tier-1 file referenced by the router.
-- `Audit-CodexRouting.ps1` and `Update-CodexRouting.ps1` are the authoritative verification / regeneration tools.
-
-Failures and how to do differently:
-- The initial missing route was a real absent file, not a logical routing error.
-- Regenerate and re-audit after adding the missing file; do not assume the router snapshot is correct until the audit passes.
-
-References:
-- Audit initially reported `missing_mandatory_count: 1` for `memories/2_governance/PREFLIGHT_CHECKLIST.md`.
-- `PREFLIGHT_CHECKLIST.md` was added back with a minimal checklist.
-- After regeneration, audit returned `missing_mandatory_count: 0`, `missing_fallback_count: 0`, `missing_roots_count: 0`, `legacy_ref_count: 0`.
-- `CODEX_DYNAMIC_ROUTING.md` `Missing Entries` section now shows `- None`.
-
-### Task 5: Rate the current `.codex` setup and recommend the next improvement
-
-task: score knowledge vs skills and suggest the highest-leverage +0.5 improvement
-task_group: evaluation / recommendation
-task_outcome: success
-
-Preference signals:
-- when the user asked for “rating 1/10 for all token cost, speed, pros and cons,” they want explicit scoring.
-- when the user asked for the smallest change that could raise the rating by 0.5, they want a leverage-first recommendation.
-
-Reusable knowledge:
-- The boot layer is now relatively lean; the remaining cost lives mostly in large `skills/` files.
-- The best next improvement is selective skill loading / front-door files for large skill docs, not more boot-file shrinking.
-
-Failures and how to do differently:
-- Do not keep shrinking the boot layer once it is already stable; the bottleneck has moved to `skills/`.
-
-References:
-- `00_PULSE.md` points `ai claude` to `skills/claude/README.md` and `ai starting point` to `skills/starting-point/README.md`.
-- Largest `skills/claude` file observed: `supabase-rls-rbac-design.md` at `73912` bytes.
-- Current routing audit was clean at the end of the session.
-
-## Thread `019ef734-e7c1-7690-beac-70590fe4c55a`
-updated_at: 2026-06-25T04:26:11+00:00
-cwd: \\?\C:\Users\user\Desktop\VIPBillion
-rollout_path: C:\Users\user\.codex\sessions\2026\06\24\rollout-2026-06-24T09-18-25-019ef734-e7c1-7690-beac-70590fe4c55a.jsonl
-rollout_summary_file: 2026-06-24T01-18-20-2Q66-vipbillion_codex_routing_localhost_and_duplicate_booking_gua.md
-
----
-description: VIPBillion workflow covering Codex boot/routing, PHP localhost startup debugging, and shared admin driver-job duplicate-booking prevention across /drivers/jobs and /bookings/list; highest-value takeaway is that the user wants durable routing/knowledge capture plus shared-source-of-truth fixes for repeated booking-assignment flows.
-task: codex-boot-routing + php-localhost-startup + shared-driver-job-duplicate-booking-guards
-task_group: vipbillion-multi-project-workflow
-task_outcome: success
-cwd: C:\Users\user\Desktop\VIPBillion
-keywords: codex boot, ai read .codex knowledge, PULSE, claude-website, claude-app, php -S, router.php, localhost:8000, session_start, driver_jobs, booking_id unique, duplicate booking, unassigned bookings, Vben, Supabase, booking assignment
----
-
-### Task 1: Codex boot and skill routing
-
-task: read .codex knowledge and route by project shape
-task_group: codex boot/routing
-task_outcome: success
-
-Preference signals:
-- when the user said `ai read .codex knowledge`, and later asked to “capture all above request that i made and chat” -> future agents should treat boot/routing and memory capture as durable, user-valued work rather than one-off context.
-- when the user asked to “help me ai performance alot more” and to “automatic run the skills or knowledge if needed” -> future agents should proactively route to the right skill based on project shape without making the user restate the distinction.
-
-Reusable knowledge:
-- `ai read .codex knowledge` is a special trigger that should return only the ready sentinel after the route-first boot read.
-- In this workspace, `website-vipbillion` is the PHP/Sovereign site and `admin-vipbillion` is the Vben/Vue admin monorepo; route based on which tree is active.
-- The admin repo has its own `CLAUDE.md` conventions, while the website route is driven by PHP front-controller files like `index.php`, `router.php`, and `lib/initData.php`.
-
-Failures and how to do differently:
-- one cached Supabase skill path probe failed because the path didn’t exist; future agents should locate cached skill copies dynamically rather than hardcoding plugin hashes.
-
-References:
-- `C:\Users\user\.codex\00_PULSE.md` trigger for `ai read .codex knowledge`
-- `C:\Users\user\.codex\memories\MEMORY.md` and workspace root showing both project directories
-
-### Task 2: Website localhost startup and debugging
-
-task: run website-vipbillion on localhost and diagnose why it stopped
-task_group: php website local dev
-task_outcome: success
-
-Preference signals:
-- the user specifically wanted the website on `http://127.0.0.1:8000/` and later clarified `that i want localhost this php website website-vipbillion` -> future agents should use the PHP built-in server from the website root.
-- when the user asked why it “suddenly stop” -> future agents should first check whether the PHP process/listener is still running before assuming a code regression.
-
-Reusable knowledge:
-- The reliable local launch shape is from `C:\Users\user\Desktop\VIPBillion\website-vipbillion` with the front controller (`index.php` or `router.php`), not from a parent folder.
-- The built-in PHP server is just a process; if it exits or its shell closes, the site disappears.
-- `router.php` starts sessions and needed a writable fallback session directory (`scratch/sessions` first, then `sys_get_temp_dir()`) on this environment.
-
-Failures and how to do differently:
-- the first launch returned `500` because PHP was started from the wrong working directory / bad front-controller context.
-- some process/network probe commands timed out; use lighter checks and direct localhost requests when possible.
-
-References:
-- website root: `C:\Users\user\Desktop\VIPBillion\website-vipbillion`
-- verified command shape: `php -S 127.0.0.1:8000 index.php` from the website root
-- `router.php` session-path fallback patch
-
-### Task 3: Driver-job duplicate booking prevention across admin assignment flows
-
-task: prevent repeated booking assignment in /drivers/jobs and /bookings/list
-task_group: admin-vipbillion booking/driver assignment
-task_outcome: success
-
-Preference signals:
-- when the user asked `will not repeated.. select same booking when submit if booking is allready asign driver will also prevent submit will show error` -> future agents should enforce uniqueness at submit time with a friendly message, not rely only on UI filtering.
-- when the user asked for `only show non assigned drivers booking` and wanted it on both `/drivers/jobs` and `/bookings/list` -> future agents should treat shared assignment flows as one source of truth and apply the same rule everywhere.
-- when the user said `yes for both all assign driver modules please check booking never repeat same booking option` -> this is a strong default for cross-screen consistency.
-
-Reusable knowledge:
-- Both `/drivers/jobs` and `/bookings/list` use the same shared driver-job create/edit drawers; the correct fix lives in the shared form/store path, not in separate page-specific implementations.
-- `driver-job.ts` now has `assertBookingAssignable(bookingId, currentJobId?)` to block duplicate assignment before insert/update.
-- `driver-job-form.vue` loads only unassigned bookings by default, but preserves the current booking in edit mode so existing assignments still render.
-- `booking-list.vue` and `booking-detail-drawer.vue` both open the same shared driver-job drawers, so a single store/form fix covers both pages.
-- The schema already has `uq_driver_jobs_booking UNIQUE (booking_id)` in migration `133_vipbillion_drivers.sql`; the UI/store guard complements the DB invariant.
-
-Failures and how to do differently:
-- filtering only in the dropdown was not enough; the store also needed a submit-time guard so the user sees a friendly error rather than a raw DB unique violation.
-- the first booking-option filter needed refinement so the currently selected booking remains visible in edit mode; preserve the active relation while hiding other assigned bookings.
-
-References:
-- `admin-vipbillion/apps/web-antd/src/stores/driver-job.ts`
-  - added `assertBookingAssignable()`
-  - create/update now call the guard
-- `admin-vipbillion/apps/web-antd/src/views/driver-jobs/driver-job-form.vue`
-  - booking options dedupe and active-booking preservation logic
-- `admin-vipbillion/apps/web-antd/src/views/bookings/booking-list.vue`
-  - `Assign Driver` opens `DriverJobCreateDrawer`
-- `admin-vipbillion/apps/web-antd/src/views/bookings/drawer/booking-detail-drawer.vue`
-  - shares the same driver-job create/edit drawers
-- `admin-vipbillion/apps/web-antd/src/sql/migrations/133_vipbillion_drivers.sql`
-  - `CONSTRAINT uq_driver_jobs_booking UNIQUE (booking_id)`
-
-### Task 4: Pause/resume unfinished admin work
-
-task: save and resume unfinished driver-job assignment work
-task_group: workflow pause/resume
-task_outcome: success
-
-Preference signals:
-- when the user asked `save first` and later `yes now ai could continue yesterday task, resume with these unfinish task` -> future agents should preserve unfinished context cleanly and resume from the same workspace state.
-
-Reusable knowledge:
-- the unfinished area stayed focused on the shared driver-job assignment flow; future resumes should re-check both create and edit drawers plus the booking picker.
-
-References:
-- unfinished files: `admin-vipbillion/apps/web-antd/src/views/driver-jobs/driver-job-form.vue`, `admin-vipbillion/apps/web-antd/src/stores/driver-job.ts`, `admin-vipbillion/apps/web-antd/src/views/bookings/booking-list.vue`
-
-## Thread `019f121e-6e58-7773-86dd-ecfdac19695d`
-updated_at: 2026-06-29T06:45:05+00:00
-cwd: \\?\C:\xampp\htdocs
-rollout_path: C:\Users\user\.codex\sessions\2026\06\29\rollout-2026-06-29T14-43-37-019f121e-6e58-7773-86dd-ecfdac19695d.jsonl
-rollout_summary_file: 2026-06-29T06-43-32-p0Jf-root_gitignore_added_for_xampp_htdocs.md
-
----
-description: Added a conservative root .gitignore for the C:\xampp\htdocs project after checking the actual layout and git status; ignored editor noise, env files, logs, vendor, uploads, and common build/cache folders while leaving source assets visible.
-task: add suitable root .gitignore for project root
-task_group: C:\xampp\htdocs / repo hygiene
-task_outcome: success
-cwd: C:\xampp\htdocs
-keywords: .gitignore, git status, uploads, vendor, api/logs, php, composer, node build outputs, workspace layout
----
-### Task 1: Add a suitable root `.gitignore`
-
-task: add suitable root .gitignore for project root
-task_group: C:\xampp\htdocs repo hygiene
-task_outcome: success
-
-Preference signals:
-- the user asked for a `.gitignore` specifically for the "project root folder" -> future agents should inspect the actual root layout before proposing ignore rules
-- the repo had mixed content (`api/`, `assets/`, `uploads/`, checked-in `vendor/`) -> future agents should be conservative and avoid broad ignores that could hide real source or required checked-in assets
-
-Reusable knowledge:
-- `git status --short` is the quickest validation here; after the patch, `vendor/`, `uploads/`, and `api/logs/` disappeared from status, confirming the ignore rules worked
-- the applied root `.gitignore` ignored: `.DS_Store`, `Thumbs.db`, `desktop.ini`, `.idea/`, `.vscode/`, `*.swp`, `*.swo`, `.env`, `.env.*`, `!.env.example`, `*.log`, `api/logs/`, `/.phpunit.result.cache`, `/vendor/`, `composer.phar`, `/node_modules/`, `/dist/`, `/build/`, `/coverage/`, `/.cache/`, `/tmp/`, `/temp/`, `/uploads/`
-- root contents included `.git`, `api/`, `assets/`, `cgi-bin/`, `uploads/`, `vendor/`, `index.html`, `loading.gif`, `no-images.jpg`, `README.md`, `vite.svg`
-
-Failures and how to do differently:
-- the main risk was over-ignoring source in a workspace that already has many generated-looking files under `assets/`; the conservative approach avoided blanket-ignoring `assets/`
-- future edits to ignore rules should be based on root layout and `git status`, not assumptions
-
-References:
-- `Get-ChildItem -Force -LiteralPath C:\xampp\htdocs | Select-Object Name,Mode,Length`
-- `git -C C:\xampp\htdocs status --short` initially showed `?? api/`, `?? assets/`, `?? index.html`, `?? loading.gif`, `?? no-images.jpg`, `?? uploads/`, `?? vendor/`, `?? vite.svg`
-- added file: `C:\xampp\htdocs\.gitignore`
-- post-patch verification: `vendor/`, `uploads/`, and `api/logs/` no longer appeared in `git status --short`
-
-## Thread `019f1246-e53e-72f0-a469-5116c511793c`
-updated_at: 2026-06-29T07:29:44+00:00
-cwd: \\?\C:\xampp\htdocs_86cara
-rollout_path: C:\Users\user\.codex\sessions\2026\06\29\rollout-2026-06-29T15-27-48-019f1246-e53e-72f0-a469-5116c511793c.jsonl
-rollout_summary_file: 2026-06-29T07-27-43-KIoY-update_homepage_meta_branding_to_lapan_enam.md
-
----
-description: Updated homepage metadata branding from Eighty Six Car Accessories to LAPAN ENAM Car Accessories in index.html and the web manifest; verified exact-match search no longer found the old brand string.
-task: change all meta-related text from Eighty Six Car Accessories to LAPAN ENAM Car Accessories
-task_group: static-site metadata update
-task_outcome: success
-cwd: C:\xampp\htdocs_86cara
-keywords: index.html, site.webmanifest, metadata, title tag, description, open graph, twitter card, pwa manifest, rg, apply_patch
----
-
-### Task 1: Update site metadata branding
-
-task: replace all meta-related brand text from Eighty Six Car Accessories to LAPAN ENAM Car Accessories in the homepage head and web manifest
-task_group: static-site metadata update
-task_outcome: success
-
-Preference signals:
-- The user asked to change “all meta related text from Eighty Six Car Accessories.... to LAPAN ENAM Car Accessories” and asked to “find all and changes here” -> future similar requests should do a full project search for brand/meta occurrences before editing.
-- The user supplied the exact new brand string “LAPAN ENAM Car Accessories” -> preserve that capitalization in metadata unless the user specifies otherwise.
-
-Reusable knowledge:
-- `index.html` contains the main head metadata for this site: title, author, application-name, apple-mobile-web-app-title, keywords, description, Open Graph, and Twitter tags.
-- `assets/favicon/site.webmanifest` also carries the app name metadata and should be updated alongside the head tags for a consistent brand rename.
-- An exact search with `rg -n -F "Eighty Six Car Accessories" .` is a reliable way to confirm no old metadata brand strings remain.
-- The rollout intentionally left canonical and OG image URL paths unchanged because they were file/domain identifiers, not direct brand text.
-
-Failures and how to do differently:
-- No edit failure occurred. If the user wants a full brand migration next time, ask whether they also want URL slugs, asset filenames, and domain references renamed, since those were left intact here.
-
-References:
-- `index.html` after patch:
-  - `<title>LAPAN ENAM Car Accessories | Premium Auto Parts & Accessories</title>`
-  - `<meta name="author" content="LAPAN ENAM Car Accessories" />`
-  - `<meta name="application-name" content="LAPAN ENAM Car Accessories" />`
-  - `<meta name="apple-mobile-web-app-title" content="LAPAN ENAM Car Accessories" />`
-  - keyword/description/OG/Twitter fields updated to `LAPAN ENAM Car Accessories`
-- `assets/favicon/site.webmanifest` after patch:
-  - `"name": "LAPAN ENAM Car Accessories"`
-  - `"short_name": "Lapan Enam"`
-- Verification evidence: after patch, `rg -n -F "Eighty Six Car Accessories" .` exited with code 1 (no matches).
-
-## Thread `019f1256-24bf-7820-938f-97654486fdbe`
-updated_at: 2026-06-29T07:47:00+00:00
-cwd: \\?\C:\xampp\htdocs_86cara
-rollout_path: C:\Users\user\.codex\sessions\2026\06\29\rollout-2026-06-29T15-44-28-019f1256-24bf-7820-938f-97654486fdbe.jsonl
-rollout_summary_file: 2026-06-29T07-44-23-O0PA-update_homepage_meta_branding_and_description.md
-
----
-description: Updated the homepage metadata branding in `index.html` and the manifest to the full company name, with expanded SEO/social descriptions; main durable takeaway is that this repo's live metadata is centralized in `index.html` and mirrored in `assets/favicon/site.webmanifest`.
-task: update all meta title and description text to the full company name with fuller descriptions
-task_group: php-static-marketing-site
-cwd: C:\xampp\htdocs_86cara
-keywords: index.html, site.webmanifest, meta title, meta description, og:title, og:description, twitter:title, twitter:description, branding, SEO, rg, apply_patch, regex parse error
----
-### Task 1: Update homepage SEO/social metadata
-
-task: update all meta title and description text to the full company name with fuller descriptions
-task_group: php-static-marketing-site
-task_outcome: success
-
-Preference signals:
-- when asked to use the exact full company name, the user said: "Lapan Enam Car Window Film & Acessories (M) SDN BHD" -> treat this as the preferred branding string for metadata updates in similar requests.
-- when asked about descriptions, the user said: "update all description to alittle more complete" -> future metadata edits should expand descriptions across SEO/social tags instead of leaving them minimal.
-
-Reusable knowledge:
-- `index.html` is the main live metadata entry point in this repo; the `<head>` contains the page title, description, keywords, Open Graph, Twitter, and app-name fields.
-- `assets/favicon/site.webmanifest` mirrors the branding and should be updated alongside `index.html` when the site name changes.
-- The existing site metadata was replaced successfully in one surgical patch; no broader refactor was needed.
-
-Failures and how to do differently:
-- The first regex-based `rg` search failed with `regex parse error ... unclosed group`; rerun with a simpler literal search pattern instead of a more complex alternation when searching for HTML meta tags in PowerShell.
-- `PROJECT_CONTEXT.md` was not present, so there was no additional repo fingerprint file to consult.
-
-References:
-- Before edit, `index.html` had `LAPAN ENAM Car Accessories | Premium Auto Parts & Accessories` in `<title>` and matching `og:*` / `twitter:*` text.
-- After edit, `index.html` uses `Lapan Enam Car Window Film & Acessories (M) SDN BHD` in `<title>`, `meta author`, `application-name`, `apple-mobile-web-app-title`, `description`, `og:title`, `og:description`, `twitter:title`, and `twitter:description`.
-- `assets/favicon/site.webmanifest` `name` was updated to `Lapan Enam Car Window Film & Acessories (M) SDN BHD`.
-- Verification command: `rg -n -S 'LAPAN ENAM Car Accessories|Premium Auto Parts & Accessories|Discover top-quality car accessories|Upgrade your ride with LAPAN ENAM Car Accessories' .\index.html .\assets\favicon\site.webmanifest` returned no matches after the patch.
-- Quick readback of the edited head section showed the new company name consistently applied across the metadata block.
-
-## Thread `019f1683-3a1e-7ec2-9ace-96c9a1ca52e1`
-updated_at: 2026-06-30T03:18:41+00:00
-cwd: \\?\C:\Users\user\.codex
-rollout_path: C:\Users\user\.codex\sessions\2026\06\30\rollout-2026-06-30T11-12-12-019f1683-3a1e-7ec2-9ace-96c9a1ca52e1.jsonl
-rollout_summary_file: 2026-06-30T03-12-06-Hjah-push_to_github_failed_http_408_with_skills_sync_hook.md
-
----
-description: Local Codex repo was one commit ahead of origin/main; push to GitHub failed with HTTP 408 while a push hook also synced skills/ to a second remote. New local edit appeared in rules/default.rules after the failed push.
-task: push local changes to GitHub and diagnose why the repo was not updating
-task_group: git/github push troubleshooting
- task_outcome: fail
-cwd: C:\Users\user\.codex
-keywords: git push, origin/main, HTTP 408, RPC failed, send-pack, branch ahead 1, skills-origin, rules/default.rules, PowerShell, remote sync hook
----
-### Task 1: Push local changes to GitHub
- task: diagnose why git said everything up-to-date and push local commit to origin/main
-task_group: git/github push troubleshooting
-task_outcome: fail
-
-Preference signals:
-- when the user said `i see what is this happening and i want to save to github` then `retry`, they wanted diagnosis plus retry in the same session rather than just explanation.
-
-Reusable knowledge:
-- `git rev-list --left-right --count origin/main...HEAD` gave `0\t1`, confirming one commit ahead locally.
-- `git branch -vv` showed `main 72fcab8 [origin/main: ahead 1] save`.
-- `git push origin main` triggered extra automation: `📤 Also pushing skills/ to ZetaTech-zt...` and `✅ Skills synced to ZetaTech-zt`.
-- Push failure was network/remote-side: `HTTP 408` / `send-pack: unexpected disconnect while reading sideband packet` / `fatal: the remote end hung up unexpectedly`.
-- `git remote -v` revealed two remotes: `origin` at `https://github.com/georgexin01/codex-ai-skills.git` and `skills-origin` at `https://github.com/ZetaTech-zt/skills-codex-xin.git`.
-
-Failures and how to do differently:
-- The push did not complete; the branch remained `ahead 1`.
-- A subsequent `git status` showed an unrelated local change in `rules/default.rules`, so re-check working tree after any failed push before retrying.
-- `git remote show origin` could not connect to GitHub (`Failed to connect to github.com port 443 after 37 ms`), so network instability should be considered before repeated retries.
-
-References:
-- `git status --short --branch`
-- `git log --oneline --decorate -n 5`
-- `git rev-list --left-right --count origin/main...HEAD`
-- `git branch -vv`
-- `git remote -v`
-- `git remote show origin`
-- `git diff -- rules/default.rules`
-- Error string: `error: RPC failed; HTTP 408 curl 22 The requested URL returned error: 408`
-
-## Thread `019f1724-ce85-70b2-9a6e-a4bd83b30031`
-updated_at: 2026-06-30T06:10:44+00:00
-cwd: \\?\C:\Users\user\Desktop\New folder\thongthaimassage_v2
-rollout_path: C:\Users\user\.codex\sessions\2026\06\30\rollout-2026-06-30T14-08-43-019f1724-ce85-70b2-9a6e-a4bd83b30031.jsonl
-rollout_summary_file: 2026-06-30T06-08-35-x1Vn-trace_bootstrap_modal_source_reservation_page.md
-
----
-description: Traced the reservation page modal source: the `.modal('show')` call in `src/admin/reservation.js` opens the Bootstrap modal element declared in `admin/reservation.php`, with Bootstrap/jQuery bundled in `admin/js/reservation_vendor.min.js`.
-task: trace where `.modal` comes from in reservation.js
-task_group: php-admin-reservation-ui
-task_outcome: success
-cwd: C:\Users\user\Desktop\New folder\thongthaimassage_v2
-keywords: bootstrap modal, jquery, reservation.js, reservation.php, reservation_vendor.min.js, open-floor-plan, PowerShell quoting, modal plugin
----
-### Task 1: Trace modal provenance
-
-task: identify source of `$('.modal').modal('show')` in reservation flow
-task_group: php-admin-reservation-ui
-task_outcome: success
-
-Preference signals:
-- when the user asked `find this .modal where are they come from`, they wanted provenance/source tracing, not a generic explanation.
-- the user pointed at the exact snippet in `src/admin/reservation.js`, so future similar debugging should start from the selected code and follow the HTML/template + bundled JS.
-
-Reusable knowledge:
-- `admin/reservation.php` contains the actual modal markup: `<div class="modal fade" tabindex="-1" data-backdrop="static" data-keyboard="false">` at line 308.
-- `src/admin/reservation.js` binds the floor-plan click handler at lines 376-380 and calls `$('.modal').modal('show');`.
-- `admin/reservation.php` loads both `./js/reservation_vendor.min.js` and `./js/reservation.min.js` at the bottom of the page.
-- `admin/js/reservation_vendor.min.js` includes Bootstrap 4.6.2 and jQuery 3.7.1, so `.modal()` is Bootstrap’s jQuery plugin.
-
-Failures and how to do differently:
-- The first `rg` attempt hit a PowerShell quoting/parser error; use simpler patterns or targeted `Select-String`/`Get-Content` slices in this repo when searching from PowerShell.
-- Broad vendor-bundle greps can time out because the minified file is large; use known version strings or the page markup instead.
-
-References:
-- `src/admin/reservation.js:376-380` `$(document).on('click', '.open-floor-plan', function () { ... $('.modal').modal('show'); });`
-- `admin/reservation.php:308-320` modal markup block beginning with `<div class="modal fade" ...>` and containing `<div class="modal-dialog modal-xl modal-map modal-dialog-centered modal-dialog-scrollable">`.
-- `admin/reservation.php:345-346` script includes for `reservation_vendor.min.js` and `reservation.min.js`.
-- `admin/js/reservation_vendor.min.js` strings observed in output: `VERSION...4.6.2`, `t.Modal=Ee`, `jQuery JavaScript Library v3.7.1`.
-
-## Thread `019f1732-11d7-7a81-98b4-3f324af1bc0b`
-updated_at: 2026-06-30T06:27:29+00:00
-cwd: \\?\C:\Users\user\Desktop\New folder\admin-panel-Thongthai
-rollout_path: C:\Users\user\.codex\sessions\2026\06\30\rollout-2026-06-30T14-23-10-019f1732-11d7-7a81-98b4-3f324af1bc0b.jsonl
-rollout_summary_file: 2026-06-30T06-23-05-5JOj-fix_vite_tsconfig_and_broken_table_primary_css.md
-
----
-description: Fixed a Vite/TypeScript build failure in a Vue 3 admin panel, then traced and repaired a separate malformed `.table-primary` CSS rule in both SCSS source and generated CSS; final build passed and the user asked to preserve design/regression safety.
-task: fix npm run build errors and verify admin panel styling is not broken
-task_group: C:\Users\user\Desktop\New folder\admin-panel-Thongthai
-task_outcome: success
-cwd: C:\Users\user\Desktop\New folder\admin-panel-Thongthai
-keywords: vite, typescript, moduleResolution, bundler, __dirname, fileURLToPath, lightningcss, css-post, table-primary, bootstrap.scss, tsconfig.json, vite.config.ts, npm run build
----
-
-### Task 1: Fix Vite/TypeScript build errors
-
-task: resolve `npm run build` errors caused by Vite/TS config
-task_group: Vue admin panel build
-task_outcome: success
-
-Preference signals:
-- user asked: "ai check i npm run build but some error happen see can this be fixed?" -> future similar build failures should be investigated and fixed, not just explained.
-- user later asked: "make sure these change wont change any design or others broken my admin panel" -> future fixes should be verified for regression risk, especially visual/design impact.
-
-Reusable knowledge:
-- Repo is an ESM Vite app; `vite.config.ts` was type-checked with `moduleResolution: "node"`, which caused `vite` package resolution errors. Switching to `moduleResolution: "bundler"` and adding `node` types fixed the config/typecheck side.
-- In `vite.config.ts`, `__dirname` must be derived with `path.dirname(fileURLToPath(import.meta.url))` in this project.
-
-Failures and how to do differently:
-- The first build failure was only the TypeScript/Vite layer; a second CSS minification error surfaced after that. Future agents should re-run the full build after each fix instead of assuming the first error was the only blocker.
-
-References:
-- `tsconfig.json` line 7: `"moduleResolution": "bundler"`
-- `tsconfig.json` line 15: `"types": ["vite/client", "node"]`
-- `vite.config.ts` lines 4-8: `import { fileURLToPath } from 'url'` and `const __dirname = path.dirname(fileURLToPath(import.meta.url))`
-- original errors: `Cannot find module 'vite'`, `Cannot find module 'path'`, `Cannot find name '__dirname'`
-
-### Task 2: Fix broken table-primary CSS and re-verify build
-
-task: repair malformed CSS emitted by the theme table mixin and confirm no regression
-task_group: CSS/theme build
-task_outcome: success
-
-Preference signals:
-- user asked whether the change would "change any design" or "others broken" their admin panel -> future similar edits should preserve existing theme behavior and be validated against build output.
-
-Reusable knowledge:
-- `src/main.ts` imports `src/style.scss`, and `src/style.scss` imports the generated CSS bundle (`src/assets/css/styles.css`), so build-time CSS errors can come from generated CSS, not only SCSS sources.
-- The broken CSS came from `.table-primary`; the typo was also present in the generated CSS bundle, so both the source SCSS and the generated CSS had to be corrected to keep source and consumed output aligned.
-- After correcting `.table-primary` back to `var(--primary-rgb)` / `rgba(var(--primary-rgb), ...)`, `npm run build` completed successfully.
-
-Failures and how to do differently:
-- A PowerShell line-range attempt using `Select-Object -Index 220..245` failed; use `-Skip N -First M` for reading a file window in PowerShell.
-- Build still produced non-fatal warnings about unresolved static image URLs and large chunks; these did not indicate a regression in the fix and were not changed.
-
-References:
-- `src/main.ts` imports `./style.scss`
-- `src/style.scss` imports `./assets/css/bootstrap.css`, `./assets/css/styles.css`, `./assets/css/icons.css`
-- `src/assets/scss/bootstrap/_tables.scss:233-234` fixed from `@include table-color(var(-rgb(100, 85, 85)mary-rgb));` to `@include table-color(var(--primary-rgb));`
-- `src/assets/css/styles.css:8728-8746` fixed `.table-primary` variables from malformed `var(-rgb(100, 85, 85) mary-rgb)` to `var(--primary-rgb)`
-- final verification: `npm run build` succeeded and emitted `dist/` artifacts
-
 ## Thread `019f1d22-a3ef-7710-830f-87fa8a26a443`
 updated_at: 2026-07-01T11:08:01+00:00
 cwd: \\?\C:\Users\user\.codex
diff --git a/rollout_summaries/2026-06-18T10-09-14-L9so-kushiro_api_domain_swap_localhost_preserved.md b/rollout_summaries/2026-06-18T10-09-14-L9so-kushiro_api_domain_swap_localhost_preserved.md
deleted file mode 100644
--- a/rollout_summaries/2026-06-18T10-09-14-L9so-kushiro_api_domain_swap_localhost_preserved.md
+++ /dev/null
@@ -1,43 +0,0 @@
-thread_id: 019eda34-cf7c-7a73-a898-44036512f0ac
-updated_at: 2026-06-18T10:11:50+00:00
-rollout_path: C:\Users\user\.codex\sessions\2026\06\18\rollout-2026-06-18T18-09-23-019eda34-cf7c-7a73-a898-44036512f0ac.jsonl
-cwd: \\?\C:\xampp\htdocs
-git_branch: main
-
-# Updated Kushiro API runtime domains to `kushiro.my`
-
-Rollout context: The user asked to update the Kushiro PHP API so development can stay on `localhost`, while staging/published environments use the official site domain `kushiro.my` instead of `kushirobbq.com.my` (and related old hostnames).
-
-## Task 1: Update Kushiro API domain routing and upload endpoints
-
-Outcome: success
-
-Preference signals:
-- The user said: "dev is ok.. localhost.. but publish or staging are using kushiro.my this is my official website" -> future updates should preserve local `localhost` behavior while changing only published/staging hostnames.
-- The user clarified: "so for any kushirobbq.com.my better to help me update to kushiro.my" -> treat `kushirobbq.com.my` as a replacement target across the Kushiro workspace when the request is about the public API/site domain.
-
-Key steps:
-- Searched the workspace for domain touchpoints with `rg` on `kushirobbq`, `kushiro.my`, `localhost`, and `127.0.0.1`.
-- Identified the main runtime references in `api/KushiroBBQ/Config.php`, `api/KushiroBBQ/Helper.php`, `api/upload_receiver.php`, and `api/KushiroBBQ/Controllers/RedirectController.php`.
-- Patched staging/production origin lists and production/staging URL fields to `https://kushiro.my`.
-- Updated VPS upload URL/domain constants and host matching to `kushiro.my` / `api.kushiro.my`.
-- Updated the upload receiver’s published path and returned file URL to `kushiro.my`.
-- Updated the default redirect target to `https://kushiro.my/`.
-- Verified syntax with `php -l` on all 4 edited files and reran search to confirm no remaining live `kushirobbq.com.my` runtime references in the workspace.
-
-Failures and how to do differently:
-- The assistant initially had to search before editing because the relevant domain wiring was scattered across config, helper, upload, and redirect code. Future similar requests should start with a domain sweep before patching.
-- The upload receiver’s filesystem path was changed based on the new domain name; the assistant noted this should be double-checked against the actual server directory layout if deployment differs.
-
-Reusable knowledge:
-- In this repo, environment selection is driven by `api/KushiroBBQ/.env` / `APP_ENV`, and the main environment config lives in `api/KushiroBBQ/Config.php`.
-- The runtime domain touchpoints that matter for a public domain swap are: CORS `allowedOrigins`, `cookieDomain`, public `domain` / `attachmentUrl`, upload receiver URL/path, host-based corporate ID routing, and redirect default links.
-- `php -l` is a quick useful validation step after text-only PHP config edits.
-
-References:
-- [1] Search result showed the main old-domain hits, including `api/KushiroBBQ/Config.php`, `api/KushiroBBQ/Helper.php`, `api/upload_receiver.php`, and `api/KushiroBBQ/Controllers/RedirectController.php`.
-- [2] `php -l api\KushiroBBQ\Config.php` -> `No syntax errors detected`
-- [3] `php -l api\KushiroBBQ\Helper.php` -> `No syntax errors detected`
-- [4] `php -l api\upload_receiver.php` -> `No syntax errors detected`
-- [5] `php -l api\KushiroBBQ\Controllers\RedirectController.php` -> `No syntax errors detected`
-- [6] `git diff` confirmed the exact replacements from `kushirobbq.com.my` / `kushirobbq.zetacapital.my` to `kushiro.my` in the four files above.
diff --git a/rollout_summaries/2026-06-23T01-21-40-OmVb-codex_cleanup_routing_checkpoint_skill_frontdoors.md b/rollout_summaries/2026-06-23T01-21-40-OmVb-codex_cleanup_routing_checkpoint_skill_frontdoors.md
deleted file mode 100644
--- a/rollout_summaries/2026-06-23T01-21-40-OmVb-codex_cleanup_routing_checkpoint_skill_frontdoors.md
+++ /dev/null
@@ -1,160 +0,0 @@
-thread_id: 019ef211-9b91-7493-8298-29bbba8d21d1
-updated_at: 2026-06-23T03:55:01+00:00
-rollout_path: C:\Users\user\.codex\sessions\2026\06\23\rollout-2026-06-23T09-21-47-019ef211-9b91-7493-8298-29bbba8d21d1.jsonl
-cwd: \\?\C:\Users\user\.codex
-git_branch: main
-
-# The user repeatedly asked to shrink `.codex` knowledge/routing overhead, then validated routing after the cleanup and asked for a rating-based review of what to improve next.
-
-Rollout context: The workspace was `C:\Users\user\.codex`. The user started by asking to inspect `.codex` knowledge, then to identify waste/legacy content, then to trim safely, then to verify routing after edits, then to rate the current setup and recommend a small improvement to raise the rating. The rollout included several safe edits to `.codex` boot/routing files and memory recipes, followed later by a routing audit that found a missing mandatory file and was fixed by restoring the missing checklist. There was also a brief failed attempt to over-trim `memories/MEMORY.md` that had to be restored from `git show HEAD`.
-
-## Task 1: Reduce duplicate `.codex` boot/routing prose
-Outcome: success
-
-Preference signals:
-- when the user asked to find “10 parts that are a waste or unnecessary content” in `.codex` knowledge/memories/skills, this suggests they want the boot path to stay lean and duplicate wording removed by default.
-- when the user later asked “change by your recommendation” and kept confirming step-by-step, this suggests they are willing to let the agent make incremental cleanup passes as long as each pass is explained and verified.
-- when the user asked to “show me comparison tables on what changes and rating 1/10,” this suggests they want before/after comparison tables and an explicit rating for each cleanup.
-
-Key steps:
-- The agent read `00_PULSE.md`, `AGENTS.md`, `00_CODEX_START_HERE.md`, `CODEX_DYNAMIC_ROUTING.md`, and `memories/MEMORY.md` to identify duplicate routing/boot material.
-- `00_CODEX_START_HERE.md` was reduced from a full secondary boot manual into a short deferred reference.
-- `AGENTS.md` was trimmed into a compact bridge that points to `00_PULSE.md` instead of repeating the boot/manual content.
-- `CODEX_DYNAMIC_ROUTING.md` was compressed into a fallback routing snapshot instead of a long generated change log.
-- The top of `memories/MEMORY.md` was reduced so it points to `00_PULSE.md` and `AGENTS.md` rather than repeating many always-on protocol bullets.
-
-Failures and how to do differently:
-- A patch against `memories/MEMORY.md` initially failed because the file’s text encoding / punctuation didn’t match the patch hunk exactly.
-- A later cleanup attempt accidentally over-trimmed `memories/MEMORY.md` and had to be restored from `git show HEAD:memories/MEMORY.md`.
-- Future similar edits should use smaller, line-preserving replacements and immediately read back the file after each change.
-
-Reusable knowledge:
-- `00_PULSE.md` is the authoritative single boot read for `.codex` routing.
-- For `.codex` routing, the agent should stop at the first valid trigger match and avoid over-reading additional files during normal boot.
-- The user likes comparison tables with estimated token cost, speed, speed increase %, and rating.
-- `memories/raw_memories.md`, rollout summaries, and archive/history should stay out of normal routing.
-
-References:
-- [1] `00_CODEX_START_HERE.md` rewritten into a short deferred reference; verified length dropped to `1407` bytes.
-- [2] `AGENTS.md` rewritten into a compact bridge; verified length `2913` bytes.
-- [3] `CODEX_DYNAMIC_ROUTING.md` compressed to a fallback snapshot; verified length `1535` bytes at one point, later regenerated after routing fixes.
-- [4] `memories/MEMORY.md` top section trimmed to two lines pointing at `00_PULSE.md` and `AGENTS.md`; later restored after an over-trim, then re-verified as valid markdown.
-
-## Task 2: Convert broad skills to README-first front doors
-Outcome: success
-
-Preference signals:
-- when the user asked “so ai see how can this be fixed? change by your recommendation.. then show me comparison tables on what changes and rating 1/10,” they were implicitly asking for a higher-signal, lower-overhead routing structure.
-- when the user asked whether other skills could use `README.md`, then said “2” and later confirmed, they accepted a stepwise conversion of broad skills into tiny front doors.
-- when the user said “ok” repeatedly instead of asking for more explanation, that suggests they are comfortable with minimal confirmations if the task is clear.
-
-Key steps:
-- `skills/claude` was already converted earlier to README-first, so the next candidate was `skills/starting-point`.
-- The router in `00_PULSE.md` was updated so `ai starting point` now points to `skills/starting-point/README.md`.
-- `memories/2_governance/artifacts/skill_path_router.md` was updated to match the new front door.
-- A new `skills/starting-point/README.md` was added as a short routing layer that points back to `skill.md`.
-- `skills/starting-point/skill.md` was updated so its navigation map points to the README entrypoint.
-
-Failures and how to do differently:
-- None of the edits were blocked, but the agent had to make sure the README stayed short and did not duplicate the executor.
-- The main hazard here is letting the README grow into a second executor; future similar work should keep the front door tiny and delegate all workflow detail to the executor.
-
-Reusable knowledge:
-- Broad bootstrap skills benefit most from README-first routing because they are often loaded during early project setup.
-- The user prioritizes lower token waste and clearer front doors over keeping everything in one large executor file.
-
-References:
-- [1] `00_PULSE.md`: `"ai starting point":  "skills/starting-point/README.md"`.
-- [2] `memories/2_governance/artifacts/skill_path_router.md`: route entry updated to `skills/starting-point/README.md`.
-- [3] `skills/starting-point/README.md` created as a short front door that points to `skill.md`.
-- [4] `skills/starting-point/skill.md` router table updated to point at the README.
-
-## Task 3: Deep clean old `.codex` history and empty folders
-Outcome: success
-
-Preference signals:
-- when the user explicitly asked to remove “all legacy or old history content that maybe useless or too old to record” and “remove all the empty folder after all files has been removed,” they want aggressive cleanup of historical noise, but only for clearly safe archival content.
-- when the user asked whether trimming was “too much,” they revealed a strong preference for conservative, safe trimming that preserves unique rules.
-- when they later asked for “Batch with checkpoints,” they preferred checkpointed cleanup over blind bulk deletion.
-
-Key steps:
-- The agent inventoried old files in `.codex` and `memories` using a 7-day cutoff.
-- Safe deletions were performed for `memories/archive` and old `sessions` / `.sandbox` files older than one week.
-- Empty directories left behind were removed.
-- A clean routing audit afterward showed no missing mandatory entries, no missing fallback entries, no missing roots, and no legacy refs.
-
-Failures and how to do differently:
-- The first `memories/MEMORY.md` cleanup attempt overreached and caused a temporary file corruption / collapse; the file had to be restored from `git show HEAD`.
-- The cleanup workflow should be conservative and staged: remove obvious history first, then verify empty folders, then stop.
-- Do not treat every old-looking file as disposable; if it is referenced by routing, it may still be structurally important.
-
-Reusable knowledge:
-- `memories/archive` was safe to remove in this session because the user explicitly wanted deep cleanup and the folder was treated as historical content.
-- `sessions` and `.sandbox` can contain old runtime noise that is safe to clear when older than one week, but should be verified before deletion.
-- `memories/rollout_summaries` had no files older than one week at the time of cleanup, so it was preserved.
-
-References:
-- [1] `memories/archive` removed entirely.
-- [2] Old `sessions` files older than 1 week removed.
-- [3] Old `.sandbox` log files older than 1 week removed.
-- [4] Empty folders pruned, including `node_repl` and several temp/plugin directories.
-- [5] Routing audit after cleanup returned `missing_mandatory_count=0`, `missing_fallback_count=0`, `missing_roots_count=0`, `legacy_ref_count=0`.
-
-## Task 4: Repair routing after cleanup and confirm the router still works
-Outcome: success
-
-Preference signals:
-- when the user asked “make sure all the router routing are working correctly after all this edit and cleaning,” they want validation, not just edits.
-- when the user later asked “if anything wrong on routing ai please help me fix,” they wanted the agent to diagnose and repair actual breakage before claiming success.
-
-Key steps:
-- The agent ran `Audit-CodexRouting.ps1` and found one real issue: `memories/2_governance/PREFLIGHT_CHECKLIST.md` was missing even though the router referenced it.
-- A minimal `PREFLIGHT_CHECKLIST.md` was added back.
-- `Update-CodexRouting.ps1 -Quiet` regenerated `CODEX_DYNAMIC_ROUTING.md`.
-- The routing audit was rerun and returned clean: no missing mandatory entries, no missing fallback entries, no missing roots, no legacy refs.
-- `CODEX_DYNAMIC_ROUTING.md` was read back and now shows `## Missing Entries` with `- None`.
-
-Failures and how to do differently:
-- The first attempt to restore `memories/MEMORY.md` during cleanup corrupted line breaks and then collapsed the file; the agent had to restore it from git and re-write it in a line-preserving way.
-- `legacy_refs_detected=2` appears in the generated router metadata, but the actual audit is what matters for pass/fail.
-- Future routing repairs should first audit, then patch only the missing file(s), then regenerate, then audit again.
-
-Reusable knowledge:
-- The `.codex` route map depends on `memories/2_governance/PREFLIGHT_CHECKLIST.md`, `memories/0_apex/GROUND_KERNEL.md`, `memories/0_apex/KARPATHY_TIER0_PRINCIPLES.md`, and `memories/2_governance/artifacts/skill_path_router.md`.
-- The routing generator and audit scripts in `codex-router/` are the authoritative way to verify the route after edits.
-- The agent should treat a router audit failure as a concrete missing-file problem before assuming a logic bug.
-
-References:
-- [1] `Audit-CodexRouting.ps1` initially reported `missing_mandatory_count: 1` and missing `C:\Users\user\.codex\memories\2_governance\PREFLIGHT_CHECKLIST.md`.
-- [2] `PREFLIGHT_CHECKLIST.md` was restored with a minimal checklist.
-- [3] Re-running `Update-CodexRouting.ps1 -Quiet` and `Audit-CodexRouting.ps1` produced `missing_mandatory_count: 0`, `missing_fallback_count: 0`, `missing_roots_count: 0`, `legacy_ref_count: 0`.
-- [4] `CODEX_DYNAMIC_ROUTING.md` now shows `## Missing Entries` -> `- None`.
-
-## Task 5: Rate the current `.codex` setup and recommend the next improvement
-Outcome: success
-
-Preference signals:
-- when the user asked for “rating 1/10 for all token cost, speed, pros and cons,” they want an explicit scored assessment, not just advice.
-- when the user asked what to improve to increase the rating by 0.5, they want the smallest high-leverage fix, not a broad plan.
-- when the user asked “other than that?” after the largest skill-file recommendation, they wanted alternatives ranked by leverage.
-
-Key steps:
-- The agent scored the knowledge layer, skills layer, and whole `.codex` system separately.
-- The boot layer was judged strong after cleanup; the remaining cost is mostly in large `skills/` files.
-- The user was advised that the best +0.5 rating move is to split the biggest `skills/claude` files into tiny front doors plus deeper docs, and to keep `00_PULSE.md` as the only boot read.
-- When the user asked for alternatives, the agent recommended tighter trigger routing and more selective loading rather than more memory trimming.
-
-Failures and how to do differently:
-- The agent should not keep shrinking core boot files once the routing is already stable; the next gains are in skill selectivity.
-- The boot layer is no longer the bottleneck, so future effort should avoid over-optimizing `00_PULSE.md` at the expense of skill/library clarity.
-
-Reusable knowledge:
-- The user’s `.codex` is now best thought of as: lean boot layer, heavy skill layer.
-- For a small rating bump, the best lever is selective loading of big skills rather than further boot-file compression.
-- The biggest skill file named in the discussion was `skills/claude/supabase-rls-rbac-design.md`.
-
-References:
-- [1] `00_PULSE.md` is now the single boot read and points `ai claude` to `skills/claude/README.md` and `ai starting point` to `skills/starting-point/README.md`.
-- [2] The largest `skills/claude` file identified was `supabase-rls-rbac-design.md` at `73912` bytes.
-- [3] The current routing audit remained clean after the cleanup sequence.
-
diff --git a/rollout_summaries/2026-06-24T01-18-20-2Q66-vipbillion_codex_routing_localhost_and_duplicate_booking_gua.md b/rollout_summaries/2026-06-24T01-18-20-2Q66-vipbillion_codex_routing_localhost_and_duplicate_booking_gua.md
deleted file mode 100644
--- a/rollout_summaries/2026-06-24T01-18-20-2Q66-vipbillion_codex_routing_localhost_and_duplicate_booking_gua.md
+++ /dev/null
@@ -1,158 +0,0 @@
-thread_id: 019ef734-e7c1-7690-beac-70590fe4c55a
-updated_at: 2026-06-25T04:26:11+00:00
-rollout_path: C:\Users\user\.codex\sessions\2026\06\24\rollout-2026-06-24T09-18-25-019ef734-e7c1-7690-beac-70590fe4c55a.jsonl
-cwd: \\?\C:\Users\user\Desktop\VIPBillion
-
-# Multi-day VIPBillion workflow: Codex boot/routing, PHP localhost startup, and admin booking-to-driver assignment hardening
-
-Rollout context: The user worked across two related VIPBillion projects in the same workspace: `website-vipbillion` (PHP website) and `admin-vipbillion` (Vben/Vue admin monorepo). The rollout included an explicit Codex boot/routing request (`ai read .codex knowledge`), then a local PHP website launch/debug session for `http://127.0.0.1:8000/`, and then a longer admin-app task to prevent duplicate driver-job assignment from bookings and to filter booking options to only unassigned bookings.
-
-## Task 1: Codex boot/routing knowledge read
-
-Outcome: success
-
-Preference signals:
-
-- The user requested `ai read .codex knowledge` and later asked to “capture all above request that i made and chat” and identify what should be saved into `.codex` memories/skills for reuse. This suggests the user values durable routing/knowledge capture, not just one-off fixes.
-- The user’s repeated language about “auto find solution,” “automatic run the skills or knowledge if needed,” and “help me ai performance alot more” suggests a preference for future agents to proactively route to the right skill/knowledge instead of asking the user to re-specify the project type each time.
-
-Key steps:
-
-- Read `C:\Users\user\.codex\00_PULSE.md` and followed the trigger map for `ai read .codex knowledge`, which explicitly routed to a one-line ready sentinel.
-- Read `C:\Users\user\.codex\memories\MEMORY.md`, which contained the VIPBillion-specific boot/routing summary from prior work.
-- Confirmed from workspace root that this checkout contains both `admin-vipbillion` and `website-vipbillion`, which matters for future skill routing.
-- Read the project-specific orchestrators in `C:\Users\user\.codex\skills\claude-website\SKILL.md` and `C:\Users\user\.codex\skills\claude-app\SKILL.md` to determine the correct route for each subtask.
-
-Reusable knowledge:
-
-- `ai read .codex knowledge` is a special trigger that should be answered with the ready sentinel after the route-first boot read; do not summarize instead of emitting the sentinel.
-- In this workspace, `website-vipbillion` is the PHP/Sovereign site path and `admin-vipbillion` is the Vben/Vue admin path; future agents should choose the skill based on which tree the user names or opens.
-- The admin repo has its own `CLAUDE.md` with startup conventions and Vben-specific patterns, while the PHP site’s behavior is driven by `index.php`, `router.php`, `lib/initData.php`, and related template files.
-
-Failures and how to do differently:
-
-- The first `Supabase` skill path probe failed because the local cached path didn’t exist; future agents should locate cached skill copies by search rather than hardcoding a plugin hash/path.
-- The user’s request was about knowledge capture rather than code changes; future agents should recognize that some turns are meta-routing/knowledge tasks and stop after extracting reusable routing evidence.
-
-References:
-
-- [1] `C:\Users\user\.codex\00_PULSE.md` trigger: `"ai read .codex knowledge": "respond ONLY '[🟢] Agent is Ready..' — skip summaries"`
-- [2] `C:\Users\user\.codex\memories\MEMORY.md` VIPBillion routing note: admin and website are separate paths; boot read had already captured `admin-vipbillion` as a Vben admin repo and `website-vipbillion` as the PHP site.
-- [3] Workspace root listing showed both project directories under `C:\Users\user\Desktop\VIPBillion`.
-
-## Task 2: Start the PHP website on localhost and diagnose startup failures
-
-Outcome: success
-
-Preference signals:
-
-- The user specifically wanted the site to run on `http://127.0.0.1:8000/` and later clarified “that i want localhost this php website website-vipbillion,” which is a durable preference for using the PHP built-in server locally and for treating `website-vipbillion` as the active site root.
-- The user asked why it “suddenly stop” after it had been working, implying future agents should check whether the PHP server process is still alive before assuming a code regression.
-
-Key steps:
-
-- Inspected `website-vipbillion/index.php`, `router.php`, `.htaccess`, `lib/htmlhead.php`, `lib/initData.php`, `lib/visitor-tracker.php`, `lib/lang.php`, and `api/Config.php` to understand the front-controller and session/bootstrap path.
-- Found that the correct launch pattern is to run PHP from the website root with the front controller, not from an arbitrary working directory.
-- Verified that `php -S 127.0.0.1:8001 router.php` from the site root returned `200`, then relaunched the site on `127.0.0.1:8000` from the website root and verified the homepage returns `200`.
-- Patched `website-vipbillion/router.php` to use a local writable session directory fallback (`scratch/sessions` first, then `sys_get_temp_dir()`), because the initial 500s exposed a Windows session/temp-path problem when the script was launched incorrectly.
-
-Reusable knowledge:
-
-- For this PHP site, the working launch shape is from `C:\Users\user\Desktop\VIPBillion\website-vipbillion` with the front controller (`index.php` or `router.php` depending on test) rather than from a parent folder.
-- The built-in PHP server is a process, not a service; if it “suddenly stops,” the likely cause is that the process was closed/killed or the launch shell ended.
-- `router.php` starts sessions, and on this environment the session save path needed a writable project-local fallback to avoid temp-path issues.
-- The site boot path reads `seoData.php` and Supabase-backed data on page load, so runtime 500s can come from boot/data failures as well as from server startup issues.
-
-Failures and how to do differently:
-
-- The first localhost attempt returned `500` because PHP was started from the wrong working directory / with a bad front-controller setup. Future attempts should first confirm the current working directory and use the website root.
-- A CLI test showed a session temp-path permission issue, which led to the local session-path fallback patch; however, the actual functional fix for the localhost 200 response was starting PHP from the correct website root.
-- Some diagnostic commands timed out on heavier Windows networking/process checks; future agents should prefer lightweight checks and go straight to direct localhost requests or simple process existence probes.
-
-References:
-
-- [1] Website root: `C:\Users\user\Desktop\VIPBillion\website-vipbillion`
-- [2] Verified launch pattern: `php -S 127.0.0.1:8000 index.php` from the website root returned `200`.
-- [3] `router.php` patch: created a writable session path fallback using `scratch/sessions` and `sys_get_temp_dir()`.
-- [4] `lib/htmlhead.php` uses `SITE_URL` derived from the request host and includes `lib/initData.php`.
-- [5] `API_SETUP_COMPLETE.md` explicitly mentions local testing at `localhost:8000`.
-
-## Task 3: Prevent duplicate booking assignment in driver-job flows
-
-Outcome: success
-
-Preference signals:
-
-- The user explicitly asked that `/drivers/jobs` and `/bookings/list` “will not repeated.. select same booking when submit if booking is allready asign driver will also prevent submit will show error,” which is a strong reusable preference for validation before insert/update, not just UI filtering.
-- The user also asked for “only show non assigned drivers booking,” indicating that the booking picker should hide already assigned bookings by default.
-- The user wanted the same rule applied across both assign-driver entry points, which suggests a future default of sharing one source of truth rather than implementing per-screen one-offs.
-
-Key steps:
-
-- Identified that the booking list and booking detail drawer both open the shared `driver-job` create/edit drawers, so the correct place to fix duplicate assignment was the shared `driver-job` form/store path.
-- Inspected `admin-vipbillion/apps/web-antd/src/views/driver-jobs/driver-job-form.vue`, `drawer/driver-job-create-drawer.vue`, `drawer/driver-job-edit-drawer.vue`, `admin-vipbillion/apps/web-antd/src/stores/driver-job.ts`, `admin-vipbillion/apps/web-antd/src/stores/booking.ts`, and `admin-vipbillion/apps/web-antd/src/views/bookings/booking-list.vue`.
-- Added a store-side guard `assertBookingAssignable(bookingId, currentJobId?)` in `driver-job.ts` that checks whether a booking already has a driver job and throws a friendly error: `This booking already has a driver assigned. Please choose another booking.`
-- Applied that guard in both `create()` and `update()` so duplicate assignment is blocked regardless of entry point.
-- Updated `driver-job-form.vue` booking option loading to:
-  - keep only unassigned bookings in the dropdown by default,
-  - preserve the currently selected booking in edit mode so existing records still render correctly,
-  - dedupe options by booking id,
-  - keep the booking snapshot reactive so the picker reloads when the booking data arrives.
-- Confirmed the booking list already uses the shared driver-job drawer via the `Assign Driver` button and the booking-detail drawer, so no separate booking-list drawer implementation was needed.
-
-Reusable knowledge:
-
-- The booking assignment UI uses one shared driver-job form and shared driver-job store; fixing the store and shared form covers both `/drivers/jobs` and `/bookings/list`.
-- The store’s create/update methods are the right place for the duplicate-booking safety check because UI filtering alone is not sufficient.
-- The booking picker should load from `bookings` but exclude rows that already have a `driver_job` relation; in edit mode, the current booking must remain visible even if already assigned.
-- `useCreateDrawer` already converts generic Postgres unique violations (`23505`) into `Record already exists (duplicate)`; the new store guard produces a clearer booking-specific message before the DB error path is hit.
-- The Supabase schema already includes a unique constraint on `driver_jobs.booking_id` in migration `133_vipbillion_drivers.sql`, so the UI/store guard complements an existing DB-level invariant.
-
-Failures and how to do differently:
-
-- One attempt to use a wrong cached Supabase skill path failed; future agents should locate the actual cached skill path dynamically before assuming docs availability.
-- A `git diff` invocation failed because the command was run outside an initialized git working tree at that moment; for verification, use direct file reads or ensure the repo root is the active git directory.
-- The first booking-option patch needed a later refinement to preserve the currently selected booking in edit mode; future agents should test both create and edit behavior when filtering option lists that depend on relational state.
-
-References:
-
-- [1] `admin-vipbillion/apps/web-antd/src/stores/driver-job.ts`
-  - Added `assertBookingAssignable()`
-  - `create(values)` now calls `await assertBookingAssignable(values.booking_id)`
-  - `update(id, values)` now calls the same guard when `values.booking_id` is present
-- [2] `admin-vipbillion/apps/web-antd/src/views/driver-jobs/driver-job-form.vue`
-  - `loadOptions()` now builds booking options from unassigned bookings and preserves the active booking in edit mode
-  - The booking snapshot watcher was adjusted to reload options when the snapshot changes, even outside create mode
-- [3] `admin-vipbillion/apps/web-antd/src/views/bookings/booking-list.vue`
-  - `Assign Driver` opens `DriverJobCreateDrawer` via `driverJobCreateDrawerApi.setData({ booking: row, bookingId: row.id }).open()`
-- [4] `admin-vipbillion/apps/web-antd/src/views/bookings/drawer/booking-detail-drawer.vue`
-  - Uses the same shared create/edit driver-job drawers for booking-side assignment
-- [5] `admin-vipbillion/apps/web-antd/src/sql/migrations/133_vipbillion_drivers.sql`
-  - `uq_driver_jobs_booking UNIQUE (booking_id)` already exists, so duplicate prevention is also a schema invariant
-- [6] `useCreateDrawer.ts` duplicate handling already maps `23505` to a friendly duplicate message; the new guard is earlier and more booking-specific
-
-## Task 4: Resume/pause behavior for unfinished admin task
-
-Outcome: success
-
-Preference signals:
-
-- The user explicitly asked to “save first” and later to “continue yesterday task, resume with these unfinish task,” showing a preference for pausing and resuming incomplete work rather than losing context.
-- This suggests future agents should preserve the exact unfinished area and next-step plan when the user asks to stop, so it can be resumed quickly.
-
-Reusable knowledge:
-
-- The unfinished admin task remained centered on the shared driver-job assignment flow, not on a separate booking-specific implementation.
-- The user’s workflow expects the agent to continue from the same workspace state and keep edits in place across sessions.
-
-Failures and how to do differently:
-
-- The task was paused before a full build/test verification run; if the user resumes, a future agent should re-run a focused verification on both the booking list and driver-job form after the current edits.
-
-References:
-
-- `admin-vipbillion/apps/web-antd/src/views/driver-jobs/driver-job-form.vue`
-- `admin-vipbillion/apps/web-antd/src/stores/driver-job.ts`
-- `admin-vipbillion/apps/web-antd/src/views/bookings/booking-list.vue`
-- `admin-vipbillion/apps/web-antd/src/views/bookings/drawer/booking-detail-drawer.vue`
-
diff --git a/rollout_summaries/2026-06-29T06-43-32-p0Jf-root_gitignore_added_for_xampp_htdocs.md b/rollout_summaries/2026-06-29T06-43-32-p0Jf-root_gitignore_added_for_xampp_htdocs.md
deleted file mode 100644
--- a/rollout_summaries/2026-06-29T06-43-32-p0Jf-root_gitignore_added_for_xampp_htdocs.md
+++ /dev/null
@@ -1,48 +0,0 @@
-thread_id: 019f121e-6e58-7773-86dd-ecfdac19695d
-updated_at: 2026-06-29T06:45:05+00:00
-rollout_path: C:\Users\user\.codex\sessions\2026\06\29\rollout-2026-06-29T14-43-37-019f121e-6e58-7773-86dd-ecfdac19695d.jsonl
-cwd: \\?\C:\xampp\htdocs
-git_branch: main
-
-# Root `.gitignore` was added for the `C:\xampp\htdocs` project after checking the workspace layout and existing tracked/untracked files.
-
-Rollout context: the user asked to "ai later please add suitable .gitignore for this project root folder.." in `C:\xampp\htdocs`. The agent inspected the root contents, checked `git status`, read the short root `README.md`, and then added a conservative ignore file focused on obvious generated/noise artifacts without hiding source files.
-
-## Task 1: Add a suitable root `.gitignore`
-
-Outcome: success
-
-Preference signals:
-
-- The user asked for a `.gitignore` specifically for the "project root folder" -> future agents should default to checking the actual root layout before proposing ignore rules, rather than dropping in a generic template.
-- The user’s request was open-ended (“suitable”) and the repo contained mixed content (`api/`, `assets/`, `uploads/`, checked-in `vendor/`) -> future agents should be conservative and avoid broad ignores that could hide real source or required checked-in assets.
-
-Key steps:
-
-- Inspected the root with `Get-ChildItem -Force -LiteralPath C:\xampp\htdocs | Select-Object Name,Mode,Length` and found `.git`, `api/`, `assets/`, `cgi-bin/`, `uploads/`, `vendor/`, `index.html`, `loading.gif`, `no-images.jpg`, `README.md`, `vite.svg`.
-- Checked repository state with `git -C C:\xampp\htdocs status --short`; initially everything was untracked (`?? api/`, `?? assets/`, `?? index.html`, `?? loading.gif`, `?? no-images.jpg`, `?? uploads/`, `?? vendor/`, `?? vite.svg`).
-- Read `README.md`, which was only `"# webApp-86caraccessories"` and did not provide extra ignore conventions.
-- Added `C:\xampp\htdocs\.gitignore` and re-checked `git status` to confirm that `vendor/`, `uploads/`, and `api/logs/` were ignored while project files remained visible.
-
-Failures and how to do differently:
-
-- The main risk was over-ignoring source in a workspace that already has many generated-looking files under `assets/`; the conservative approach avoided that by not blanket-ignoring `assets/`.
-- The repo appears to vendor some dependencies and also contains generated/static files in the root, so future edits to ignore rules should be driven by actual repo layout and `git status`, not assumptions.
-
-Reusable knowledge:
-
-- In this project, a conservative root `.gitignore` can safely include OS/editor noise, env files, logs, Composer/PHP caches, Node build/cache folders, and `uploads/`.
-- The applied file specifically ignored: `.DS_Store`, `Thumbs.db`, `desktop.ini`, `.idea/`, `.vscode/`, `*.swp`, `*.swo`, `.env`, `.env.*` with `!.env.example`, `*.log`, `api/logs/`, `/.phpunit.result.cache`, `/vendor/`, `composer.phar`, `/node_modules/`, `/dist/`, `/build/`, `/coverage/`, `/.cache/`, `/tmp/`, `/temp/`, `/uploads/`.
-- `git status --short` after the patch showed `.gitignore` as the only new tracked change and confirmed the ignore rules took effect for `vendor/`, `uploads/`, and `api/logs/`.
-
-References:
-
-- [1] Root layout discovered via `Get-ChildItem -Force -LiteralPath C:\xampp\htdocs | Select-Object Name,Mode,Length`:
-  `.git`, `api/`, `assets/`, `cgi-bin/`, `uploads/`, `vendor/`, `index.html`, `loading.gif`, `no-images.jpg`, `README.md`, `vite.svg`
-- [2] Initial repo state via `git -C C:\xampp\htdocs status --short`:
-  `?? api/`, `?? assets/`, `?? index.html`, `?? loading.gif`, `?? no-images.jpg`, `?? uploads/`, `?? vendor/`, `?? vite.svg`
-- [3] Added file: `C:\xampp\htdocs\.gitignore`
-- [4] Verification after patch via `git status --short`:
-  `?? .gitignore`, `?? api/`, `?? assets/`, `?? index.html`, `?? loading.gif`, `?? no-images.jpg`, `?? vite.svg` (note: `vendor/`, `uploads/`, `api/logs/` no longer appeared)
-
-
diff --git a/rollout_summaries/2026-06-29T07-27-43-KIoY-update_homepage_meta_branding_to_lapan_enam.md b/rollout_summaries/2026-06-29T07-27-43-KIoY-update_homepage_meta_branding_to_lapan_enam.md
deleted file mode 100644
--- a/rollout_summaries/2026-06-29T07-27-43-KIoY-update_homepage_meta_branding_to_lapan_enam.md
+++ /dev/null
@@ -1,43 +0,0 @@
-thread_id: 019f1246-e53e-72f0-a469-5116c511793c
-updated_at: 2026-06-29T07:29:44+00:00
-rollout_path: C:\Users\user\.codex\sessions\2026\06\29\rollout-2026-06-29T15-27-48-019f1246-e53e-72f0-a469-5116c511793c.jsonl
-cwd: \\?\C:\xampp\htdocs_86cara
-git_branch: main
-
-# Updated homepage metadata branding from Eighty Six Car Accessories to LAPAN ENAM Car Accessories
-
-Rollout context: The user was in `C:\xampp\htdocs_86cara` editing `index.html` and asked to change all meta-related text from `Eighty Six Car Accessories` to `LAPAN ENAM Car Accessories`, including title/description-related fields.
-
-## Task 1: Update site metadata branding
-
-Outcome: success
-
-Preference signals:
-- The user asked to change “all meta related text from Eighty Six Car Accessories.... to LAPAN ENAM Car Accessories” and specifically wanted help to “find all and changes here” -> future similar requests should search the project for every metadata-brand occurrence rather than only editing the visible title.
-- The user said the new title/brand was “LAPAN ENAM Car Accessories” -> use that exact capitalization in meta/title fields unless the user says otherwise.
-
-Key steps:
-- Searched the repo for exact brand strings and metadata-related terms using `rg -n -F "Eighty Six Car Accessories" .` and also inspected `index.html` and `assets/favicon/site.webmanifest`.
-- Found the relevant occurrences concentrated in `index.html` head tags plus the web manifest.
-- Patched `index.html` title, author, application name, Apple web app title, keywords, description, Open Graph title/site name/description/image alt, and Twitter title/description/image alt.
-- Patched `assets/favicon/site.webmanifest` name and short_name.
-- Verified with `rg -n -F "Eighty Six Car Accessories" .` that no exact matches remained; the command exited with code 1 after the patch, indicating the string was no longer present.
-
-Failures and how to do differently:
-- No functional failure occurred, but the assistant explicitly chose not to rewrite canonical URLs or image asset paths because they were domain/path identifiers rather than brand text. If a future user wants full rebranding, check whether they also want URL slugs and asset filenames updated.
-
-Reusable knowledge:
-- In this repo, the homepage metadata lives in `index.html`, and PWA app naming also appears in `assets/favicon/site.webmanifest`.
-- `rg -n -F "Eighty Six Car Accessories" .` is an effective exact-match sweep for finding all remaining brand mentions after a rename.
-- The current canonical and OG image URLs still point to `86caraccessories.my` and `86caraccessories-og-1200x630.png`; those were intentionally left unchanged in this rollout.
-
-References:
-- [1] `index.html` head section before/after contained the main metadata strings; post-patch verification showed:
-  - `<title>LAPAN ENAM Car Accessories | Premium Auto Parts & Accessories</title>`
-  - `<meta name="description" content="Upgrade your ride with LAPAN ENAM Car Accessories..." />`
-  - `<meta property="og:site_name" content="LAPAN ENAM Car Accessories" />`
-  - `<meta name="twitter:title" content="LAPAN ENAM Car Accessories | Premium Auto Parts & Accessories" />`
-- [2] `assets/favicon/site.webmanifest` after patch:
-  - `"name": "LAPAN ENAM Car Accessories"`
-  - `"short_name": "Lapan Enam"`
-- [3] Verification: `rg -n -F "Eighty Six Car Accessories" .` returned exit code 1 after edits, confirming no exact matches remained in the project metadata text.
diff --git a/rollout_summaries/2026-06-29T07-44-23-O0PA-update_homepage_meta_branding_and_description.md b/rollout_summaries/2026-06-29T07-44-23-O0PA-update_homepage_meta_branding_and_description.md
deleted file mode 100644
--- a/rollout_summaries/2026-06-29T07-44-23-O0PA-update_homepage_meta_branding_and_description.md
+++ /dev/null
@@ -1,37 +0,0 @@
-thread_id: 019f1256-24bf-7820-938f-97654486fdbe
-updated_at: 2026-06-29T07:47:00+00:00
-rollout_path: C:\Users\user\.codex\sessions\2026\06\29\rollout-2026-06-29T15-44-28-019f1256-24bf-7820-938f-97654486fdbe.jsonl
-cwd: \\?\C:\xampp\htdocs_86cara
-git_branch: main
-
-# Updated site metadata branding across the main entry page
-
-Rollout context: The user asked to replace all meta title text with the full company name "Lapan Enam Car Window Film & Acessories (M) SDN BHD" and to make the meta descriptions a little more complete. The work was done in `C:\xampp\htdocs_86cara`.
-
-## Task 1: Update homepage SEO/social metadata
-
-Outcome: success
-
-Preference signals:
-- The user asked for the exact full company name to be used as the meta title everywhere: "Lapan Enam Car Window Film & Acessories (M) SDN BHD" -> future edits should treat this as the preferred branding string when updating page metadata.
-- The user asked to "update all description to alittle more complete" -> future metadata edits should expand descriptions rather than leaving them terse or product-only.
-
-Key steps:
-- Searched the repo for metadata tags and found the real site entry point in `index.html`.
-- Updated the page `<title>`, `meta author`, `application-name`, `apple-mobile-web-app-title`, `keywords`, `description`, Open Graph tags, and Twitter tags in one pass.
-- Verified the old title/description strings were removed from `index.html` and that the new branding text was present.
-
-Failures and how to do differently:
-- The first `rg` search used a regex quoting pattern that failed to parse; rerunning with a simpler literal search succeeded.
-- `PROJECT_CONTEXT.md` was not present in the repo, so there was no extra project fingerprint file to consult.
-
-Reusable knowledge:
-- In this project, the main live metadata appears in `index.html`; that is the key file for homepage SEO/social updates.
-- The metadata block includes `title`, standard description/keywords, `og:*`, `twitter:*`, and app-name fields; updating only one of them would leave branding inconsistent.
-- The browser/app install branding also mirrors `assets/favicon/site.webmanifest`, which should be updated alongside the HTML metadata when changing site name.
-
-References:
-- `index.html` before edit contained `LAPAN ENAM Car Accessories | Premium Auto Parts & Accessories` and description text about discovering car accessories.
-- Updated strings in `index.html`: `Lapan Enam Car Window Film & Acessories (M) SDN BHD` for title and social/app metadata.
-- Updated `assets/favicon/site.webmanifest` name to the same company branding.
-- Verification command/result: `rg -n -S 'LAPAN ENAM Car Accessories|Premium Auto Parts & Accessories|Discover top-quality car accessories|Upgrade your ride with LAPAN ENAM Car Accessories' .\index.html .\assets\favicon\site.webmanifest` returned no matches after the patch.
diff --git a/rollout_summaries/2026-06-30T03-12-06-Hjah-push_to_github_failed_http_408_with_skills_sync_hook.md b/rollout_summaries/2026-06-30T03-12-06-Hjah-push_to_github_failed_http_408_with_skills_sync_hook.md
deleted file mode 100644
--- a/rollout_summaries/2026-06-30T03-12-06-Hjah-push_to_github_failed_http_408_with_skills_sync_hook.md
+++ /dev/null
@@ -1,44 +0,0 @@
-thread_id: 019f1683-3a1e-7ec2-9ace-96c9a1ca52e1
-updated_at: 2026-06-30T03:18:41+00:00
-rollout_path: C:\Users\user\.codex\sessions\2026\06\30\rollout-2026-06-30T11-12-12-019f1683-3a1e-7ec2-9ace-96c9a1ca52e1.jsonl
-cwd: \\?\C:\Users\user\.codex
-git_branch: main
-
-# Push local Codex repo changes to GitHub; push was not completed because GitHub disconnected mid-transfer.
-
-Rollout context: The user wanted to understand why changes were not going to GitHub and asked to retry. The work happened in `C:\Users\user\.codex` with PowerShell. The repo was already one commit ahead of `origin/main`, and there was also a later-unexpected local modification in `rules/default.rules`.
-
-## Task 1: Diagnose and push local changes to GitHub
-Outcome: fail
-
-Preference signals:
-- The user said `i see what is this happening and i want to save to github` and then `retry` -> in similar push-failure situations, they want the agent to diagnose and retry the push rather than only explain the issue.
-
-Key steps:
-- Checked boot notes in `.codex/00_PULSE.md` and then inspected repo state with `git status --short --branch`, `git remote -v`, `git log --oneline --decorate -n 5`, `git rev-list --left-right --count origin/main...HEAD`, and `git branch -vv`.
-- Found the local branch `main` was `ahead 1` of `origin/main` and the local commit was `72fcab8 (HEAD -> main) save` while `origin/main` was still at `25fe799 save`.
-- Attempted `git push origin main`.
-- The push session showed an additional side effect: `📤 Also pushing skills/ to ZetaTech-zt...` and later `✅ Skills synced to ZetaTech-zt`, indicating this repo has a push hook or automation that mirrors `skills/` to a second remote during push.
-- The main push did not finish cleanly: it ended with `error: RPC failed; HTTP 408 curl 22 The requested URL returned error: 408`, `send-pack: unexpected disconnect while reading sideband packet`, `fatal: the remote end hung up unexpectedly`, followed by `Everything up-to-date`.
-- After the failed push, `git status` still showed `## main...origin/main [ahead 1]` and a new uncommitted change in `rules/default.rules`.
-
-Failures and how to do differently:
-- The initial diagnosis was correct that the repo had one local commit not on GitHub, but the actual push was interrupted by a network/remote disconnect, so the commit was not published.
-- A failed push can still leave the branch `ahead 1` and may also reveal unrelated local file changes afterward; re-check `git status` before assuming only the original commit remains.
-- In this repo, `git push origin main` may also trigger a `skills/` sync to the `ZetaTech-zt` remote, so future pushes should expect extra automation output and verify both remotes if needed.
-
-Reusable knowledge:
-- `git rev-list --left-right --count origin/main...HEAD` returned `0	1`, which confirmed exactly one local commit waiting to be pushed.
-- `git branch -vv` showed `* main 72fcab8 [origin/main: ahead 1] save`.
-- `git remote -v` showed two remotes: `origin` = `https://github.com/georgexin01/codex-ai-skills.git` and `skills-origin` = `https://github.com/ZetaTech-zt/skills-codex-xin.git`.
-- `git diff -- rules/default.rules` showed one added line: `prefix_rule(pattern=["git", "push", "origin", "main"], decision="allow")`, and Git warned `LF will be replaced by CRLF the next time Git touches it`.
-- `git remote show origin` failed with `fatal: unable to access 'https://github.com/georgexin01/codex-ai-skills.git/': Failed to connect to github.com port 443 after 37 ms: Couldn't connect to server`, reinforcing that network connectivity to GitHub was flaky during the session.
-
-References:
-- `git status --short --branch` -> `## main...origin/main [ahead 1]`
-- `git log --oneline --decorate -n 5` -> `72fcab8 (HEAD -> main) save` / `25fe799 (origin/main) save`
-- `git rev-list --left-right --count origin/main...HEAD` -> `0	1`
-- Push error: `error: RPC failed; HTTP 408 curl 22 The requested URL returned error: 408`
-- Push hook output: `📤 Also pushing skills/ to ZetaTech-zt...` and `✅ Skills synced to ZetaTech-zt`
-- New local diff path: `rules/default.rules`
-- Exact added line in diff: `prefix_rule(pattern=["git", "push", "origin", "main"], decision="allow")`
diff --git a/rollout_summaries/2026-06-30T06-08-35-x1Vn-trace_bootstrap_modal_source_reservation_page.md b/rollout_summaries/2026-06-30T06-08-35-x1Vn-trace_bootstrap_modal_source_reservation_page.md
deleted file mode 100644
--- a/rollout_summaries/2026-06-30T06-08-35-x1Vn-trace_bootstrap_modal_source_reservation_page.md
+++ /dev/null
@@ -1,42 +0,0 @@
-thread_id: 019f1724-ce85-70b2-9a6e-a4bd83b30031
-updated_at: 2026-06-30T06:10:44+00:00
-rollout_path: C:\Users\user\.codex\sessions\2026\06\30\rollout-2026-06-30T14-08-43-019f1724-ce85-70b2-9a6e-a4bd83b30031.jsonl
-cwd: \\?\C:\Users\user\Desktop\New folder\thongthaimassage_v2
-git_branch: main
-
-# Traced the `.modal('show')` call in the reservation page and identified both the HTML modal source and the Bootstrap/jQuery plugin providing the method.
-
-Rollout context: The user was inspecting `src/admin/reservation.js` in `C:\Users\user\Desktop\New folder\thongthaimassage_v2` and asked: `ai help me find this .modal where are they come from` for the handler `$('.modal').modal('show')`.
-
-## Task 1: Trace the modal source in reservation UI
-
-Outcome: success
-
-Preference signals:
-- The user asked directly for provenance — `find this .modal where are they come from` — which suggests that in similar debugging questions they want source tracing/attribution, not just a guess about what library is involved.
-- The user provided the active file and selection, implying future similar questions should start from the exact snippet the user highlighted and trace outward from there.
-
-Key steps:
-- Read the project boot note first (`$env:USERPROFILE\.codex\00_PULSE.md`) before deeper inspection.
-- Searched the repo for `.modal`, `open-floor-plan`, and related modal markup/attributes; the first `rg` command had a PowerShell quoting error, then a broader file search found likely files including `admin/reservation.php` and `admin/js/reservation_vendor.min.js`.
-- Opened `admin/reservation.php` and confirmed the modal markup is literally present in the page:
-  - `<div class="modal fade" tabindex="-1" data-backdrop="static" data-keyboard="false">`
-- Opened `src/admin/reservation.js` and found the click handler at the end of the file:
-  - `$(document).on('click', '.open-floor-plan', function () { ... $('.modal').modal('show'); });`
-- Confirmed the page loads both `./js/reservation_vendor.min.js` and `./js/reservation.min.js` at the bottom of `admin/reservation.php`.
-- Confirmed the vendor bundle contains Bootstrap 4.6.2 and jQuery 3.7.1, meaning `.modal()` is supplied by Bootstrap’s jQuery plugin, not custom code.
-
-Failures and how to do differently:
-- The first `rg` command failed because of PowerShell quoting; the useful fallback was to simplify the search and inspect the page files directly.
-- The vendor bundle search produced a very large output and timed out; for future similar checks, rely on known Bootstrap version strings or targeted file evidence rather than broad bundle greps.
-
-Reusable knowledge:
-- In this repo, the reservation modal is declared in `admin/reservation.php`, not generated by the JS handler.
-- `$('.modal').modal('show')` is Bootstrap modal syntax; in this project the plugin is bundled inside `admin/js/reservation_vendor.min.js`.
-- The reservation page loads the modal markup and the compiled JS at the bottom of the same PHP file, so tracing a UI action should inspect both the page template and the paired vendor/app bundles.
-
-References:
-- [1] `admin/reservation.php#L308`: `<div class="modal fade" tabindex="-1" data-backdrop="static" data-keyboard="false">` (actual modal element matched by `$('.modal')`).
-- [2] `src/admin/reservation.js#L376-L380`: `$(document).on('click', '.open-floor-plan', function () { ... $('.modal').modal('show'); });` (triggering call).
-- [3] `admin/reservation.php#L345-L346`: `<script src="./js/reservation_vendor.min.js"></script>` and `<script src="./js/reservation.min.js"></script>` (loads the plugin and page script).
-- [4] `admin/js/reservation_vendor.min.js`: output contained `t.Modal=Ee` and `jQuery JavaScript Library v3.7.1`, confirming Bootstrap 4.6.2 + jQuery are bundled there.
diff --git a/rollout_summaries/2026-06-30T06-23-05-5JOj-fix_vite_tsconfig_and_broken_table_primary_css.md b/rollout_summaries/2026-06-30T06-23-05-5JOj-fix_vite_tsconfig_and_broken_table_primary_css.md
deleted file mode 100644
--- a/rollout_summaries/2026-06-30T06-23-05-5JOj-fix_vite_tsconfig_and_broken_table_primary_css.md
+++ /dev/null
@@ -1,65 +0,0 @@
-thread_id: 019f1732-11d7-7a81-98b4-3f324af1bc0b
-updated_at: 2026-06-30T06:27:29+00:00
-rollout_path: C:\Users\user\.codex\sessions\2026\06\30\rollout-2026-06-30T14-23-10-019f1732-11d7-7a81-98b4-3f324af1bc0b.jsonl
-cwd: \\?\C:\Users\user\Desktop\New folder\admin-panel-Thongthai
-git_branch: main
-
-# Fixed the Vite/TypeScript build and then verified the admin panel still built successfully after the CSS correction.
-
-Rollout context: The user was in `C:\Users\user\Desktop\New folder\admin-panel-Thongthai` and reported `npm run build` failing with TypeScript/Vite config errors (`Cannot find module 'vite'`, `Cannot find module 'path'`, `Cannot find name '__dirname'`) and then asked whether the fix would change any design or break other parts of the admin panel.
-
-## Task 1: Fix build errors from Vite/TypeScript config
-
-Outcome: success
-
-Preference signals:
-- The user asked to fix the build error, and later asked, “make sure these change wont change any design or others broken my admin panel” -> future similar fixes should be verified for non-visual regressions, not just compile success.
-
-Key steps:
-- Inspected `package.json`, `tsconfig.json`, and `vite.config.ts` to confirm the repo was an ESM Vite app using `vue-tsc -b && vite build`.
-- Updated `tsconfig.json` from `moduleResolution: "node"` to `moduleResolution: "bundler"` and added `"node"` to `types`.
-- Updated `vite.config.ts` to use `fileURLToPath(import.meta.url)` plus `path.dirname(...)` for an ESM-safe `__dirname`.
-- Re-ran `npm run build` after the patch.
-
-Failures and how to do differently:
-- The first build still failed after the TypeScript/Vite fix because a separate CSS minification issue existed; do not assume the initial config error is the only blocker when a build reveals later stages of the pipeline.
-
-Reusable knowledge:
-- In this repo, Vite 8 + TypeScript config needed modern module resolution (`bundler`) for config type-checking.
-- `vite.config.ts` in this ESM project cannot rely on raw `__dirname`; use `fileURLToPath(import.meta.url)`.
-
-References:
-- `tsconfig.json:7` changed to `"moduleResolution": "bundler"`
-- `tsconfig.json:15` changed to `"types": ["vite/client", "node"]`
-- `vite.config.ts:4-8` added `fileURLToPath` and computed `__dirname`
-- Initial error text: `Cannot find module 'vite' or its corresponding type declarations`, `Cannot find module 'path'`, `Cannot find name '__dirname'`
-
-## Task 2: Fix broken CSS emitted by table-primary mixin and verify no regression
-
-Outcome: success
-
-Preference signals:
-- The user explicitly asked whether the fix would “change any design or others broken my admin panel” -> future fixes should preserve existing styling and be verified against build output.
-
-Key steps:
-- Used the build failure from `vite:css-post` / `lightningcss minify` to trace the real CSS problem.
-- Searched for the malformed `table-primary` token and found a typo in `src/assets/scss/bootstrap/_tables.scss` that compiled to broken CSS.
-- Found the same typo already present in the generated `src/assets/css/styles.css`, which is what `src/style.scss` imports through `src/main.ts`.
-- Patched both the SCSS source and generated CSS to restore `--primary-rgb` usage in `.table-primary`.
-- Re-ran `npm run build` and confirmed success.
-
-Failures and how to do differently:
-- A PowerShell `Select-Object -Index 220..245` attempt failed because `-Index` does not accept a range string; use `-Skip N -First M` for line windows in PowerShell.
-- There were non-fatal asset-path warnings during build (`... didn't resolve at build time, it will remain unchanged to be resolved at runtime`), but they did not block the build and were left untouched.
-
-Reusable knowledge:
-- This project imports `src/style.scss` from `src/main.ts`, and that stylesheet imports prebuilt CSS files; the build was consuming `src/assets/css/styles.css` directly.
-- The broken table styling was localized to `.table-primary` and the corrected values match the project’s existing primary color variable convention used elsewhere.
-- After the fixes, `npm run build` completed successfully, so the change behaved as a repair, not a redesign.
-
-References:
-- `src/main.ts` imports `./style.scss`
-- `src/style.scss` imports `./assets/css/bootstrap.css`, `./assets/css/styles.css`, `./assets/css/icons.css`
-- `src/assets/scss/bootstrap/_tables.scss:233-234` fixed to `@include table-color(var(--primary-rgb));`
-- `src/assets/css/styles.css:8728-8746` fixed `.table-primary` variables back to `rgba(var(--primary-rgb), ...)`
-- Final verification: `npm run build` succeeded, producing `dist/` artifacts and only warnings about unresolved static image URLs and large chunk sizes
diff --git a/skills/angel-interior-local-dev/SKILL.md b/skills/angel-interior-local-dev/SKILL.md
--- a/skills/angel-interior-local-dev/SKILL.md
+++ b/skills/angel-interior-local-dev/SKILL.md
@@ -1,5 +1,5 @@
 ---
-name: angel-interior-local-dev
+name: angel-interior-local-dev-skill
 description: Start the Angel Interior website and admin panel locally with the verified commands when `run-local.bat` exits or the user asks to run/check localhost.
 argument-hint: "[website|admin|both]"
 user-invocable: false
```
