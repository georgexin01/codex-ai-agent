# Vben table column alignment rule

- When adding a new column to an existing Vben table, update the body cells, summary row cells, and horizontal scroll width together.
- Keep the action, checkbox, and summary offsets aligned after inserting or removing columns.
- Reuse the related existing CRUD module instead of duplicating it when the new column or action only exposes the same record workflow.
