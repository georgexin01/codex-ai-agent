# VIPBillion module reuse preference

- Prefer reusing the existing booking-related CRUD modules and drawers when the workflow already exists.
- Avoid recreating or duplicating the same module unless the data flow or UI behavior is genuinely different.
- For Booking Management/report views, share the existing booking CRUD path as much as possible.
