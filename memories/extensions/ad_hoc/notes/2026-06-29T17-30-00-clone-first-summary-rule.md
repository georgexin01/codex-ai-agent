Clone-first summary rule for future Codex runs:

- If the user says "copy and paste", "duplicate", "clone", or "same modules", first find the exact source module/file to copy.
- Duplicate the whole module structure before editing anything.
- Do not reinterpret, redesign, or rebuild the layout from scratch when the user wants an exact clone.
- The cloned module should preserve the same shell, spacing, drawer/grid behavior, and component structure.
- Only after the clone exists should any inner content be changed for the new use case.
- For driver-job relation work, the correct source clone is the driver-job list drawer/module shell.
- Keep this note compact so `ai read .codex knowledge` can load it quickly, including on smaller models.
