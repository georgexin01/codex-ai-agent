Clone-first rule for future Codex work:

- When the user says "copy and paste" or "clone", first locate the exact module/file they want duplicated.
- Duplicate it directly before making any edits.
- Do not redesign or reinterpret the layout before the clone exists.
- After the clone is created, only then start changing the inside content the user requested.
