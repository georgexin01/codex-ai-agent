Record for future Codex routing:

- When the user asks for "copy and paste", "duplicate", or "same modules", treat it as a request to duplicate the whole module first.
- Use the duplicated file as the new base, then edit only the inside content the user wants changed.
- Prefer exact structural cloning over re-creating the layout from scratch when the user wants the same design.
- For driver-job relation drawer work, duplicate the existing drawer module shell first, then swap the inner table/content from driver jobs to bookings.
